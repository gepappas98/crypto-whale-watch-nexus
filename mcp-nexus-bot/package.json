{
  "name": "nexus-bot-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing the Nexus bot bridge (grids, arbitrage, portfolio) to MCP-compatible AI clients, with the same server-side gates the app's own UI goes through.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "nexus-bot-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  }
}
