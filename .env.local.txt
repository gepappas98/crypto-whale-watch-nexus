# Whale Radar — local environment (gitignored, safe to keep real values here)
#
# Filled in automatically from supabase/config.toml's project_id
# (piywvcmebdrxwhujphqp) — this is NOT a secret, it's just the project
# reference baked into the URL, so it was safe to derive without you
# pasting anything.
VITE_SUPABASE_URL=https://piywvcmebdrxwhujphqp.supabase.co
VITE_SUPABASE_PROJECT_ID=piywvcmebdrxwhujphqp

# ⚠️ THE ONE THING I COULDN'T FILL IN — genuinely not present anywhere in
# the repo (confirmed by searching every .ts/.tsx/.toml/.json/.md file).
# This is your project's PUBLIC anon/publishable key — safe to expose in
# client code (it's not the service_role key), but I have no way to see
# your Supabase dashboard, so you have to paste it yourself:
#
#   Supabase dashboard → your project → Settings → API
#   → copy "anon" / "public" key (starts with "eyJ...")
#
# OR, if this app is connected to Supabase through Lovable's own UI
# ("Connect Supabase" button), Lovable usually injects this automatically
# in Project Settings → Environment Variables — check there first, since
# you may not need to do anything manual at all in that case.
VITE_SUPABASE_ANON_KEY=

# ── Optional — only needed if you're also running the Express server (server/) ──
# Generate with: openssl rand -hex 32 — must match on both server and client.
# API_AUTH_TOKEN=
# VITE_API_TOKEN=
