# MCP framework
fastmcp>=2.0.0

# Web framework
fastapi>=0.115.0
uvicorn>=0.34.0

# HTTP client
httpx>=0.28.0

# HTML parsing (SEC EDGAR fallback)
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Crypto signing (Revolut Open Banking mTLS)
pynacl>=1.5.0

# Cache
redis>=5.0.0

# Database (SaaS layer — falls back to SQLite if asyncpg/DATABASE_URL absent)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.20.0

# Utils
python-dotenv>=1.0.0
