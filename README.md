# 🐋 Whale Radar (crypto-whale-watch-nexus) — v9.35

![Version](https://img.shields.io/badge/version-9.35-blue)
![React](https://img.shields.io/badge/React-18.3-61DAFB?logo=react)
![TypeScript](https://img.shields.io/badge/TypeScript-5.8-3178C6?logo=typescript)
![Vite](https://img.shields.io/badge/Vite-5.4-646CFF?logo=vite)

Real-time crypto intelligence platform: whale-transaction tracking, market-manipulation detection, Hyperliquid perps analytics, orderflow scanning, and an AI "council" of trading agents — built as a React/TypeScript SPA with an optional Express + Postgres backend.

**Live:**
- Lovable: https://crypto-whale-watch-nexus.lovable.app
- Vercel: https://crypto-whale-watch-nexusv9.vercel.app

---

## 📑 Table of Contents

- [What's New](#-whats-new)
- [Core Features](#-core-features)
- [Nexus Suite](#-nexus-suite-nexus)
- [Protection Engine](#-protection-engine)
- [Connecting a Trading Bot](#-connecting-a-trading-bot)
- [Trading Hub](#-trading-hub-trading-hub)
- [Hyperliquid Module](#-hyperliquid-module)
- [AI Council](#-ai-council)
- [MCP Tools (AI Agent Access)](#-mcp-tools-ai-agent-access)
- [Order Flow Scanner](#-order-flow-scanner-orderflow)
- [Reliability & Resilience Engineering](#-reliability--resilience-engineering)
- [PWA & Performance](#-pwa--performance)
- [Backend (optional)](#-backend-optional)
- [Tech Stack](#-tech-stack)
- [Project Structure](#-project-structure)
- [Getting Started](#-getting-started)
- [Environment Variables](#-environment-variables)
- [Deployment Notes](#-deployment-notes)
- [Roadmap](#-roadmap)
- [Disclaimer](#-disclaimer)

---

## 🆕 What's New

- **11th regime signal: ETH/BTC relative strength — closes another Strategic Direction P1 item** (v9.35): new `ethBtcStrengthSignal()` in `signals.ts` reads Binance's `ETHBTC` daily klines directly (one request, no need to compute a ratio from two separate USDT pairs) and scores the 7-day % change in the ETH/BTC ratio. This is a second, independent rotation signal alongside BTC dominance — dominance is BTC vs the *whole* market's total cap (can move on a stablecoin or altcoin-basket effect), while this is BTC vs specifically ETH, crypto's #2 asset and closest thing to a risk-on/risk-off bellwether pair. Wired into `SignalId` (types.ts), `DEFAULT_WEIGHTS` (weight 8, weights.ts), and `collectSignals()`. The "Dominance" family from v9.33 is renamed **"Rotation"** and now holds both `btc_dominance` and `eth_btc_strength` — this is the original 6-family name from the earlier audit's proposal, applied now that there are actually two rotation signals to group under it; confirmed no other file hardcoded the old `'dominance'` family-id string (`RegimePanel.tsx`/`engine.ts` both iterate `FAMILY_ORDER` dynamically, so the rename was a two-file change: `families.ts` + this entry). Cross-checked programmatically that `SignalId`, `DEFAULT_WEIGHTS`, and `SIGNAL_FAMILIES` all list the exact same 11 signal ids with no gaps or extras before shipping this.
- **Backtests the regime engine's own historical calls against real forward BTC price — closes the last open P1 item from Strategic Direction** (v9.34): new `src/lib/regime/backtest.ts` walks persisted regime history for every point `confirmedRegime` actually changed (a genuine new "call", not every 5-minute reconfirm tick — those would massively over-count), fetches real BTC close prices at call-time and +1h/+4h/+24h from Binance, and reports per-regime average forward return and hit rate. Hit rate is only scored for regimes with a directional claim (BULL-family: positive return counts as a hit; BEAR-family: negative) — NEUTRAL/RECOVERY are excluded rather than scored 0, since "no claim made" isn't "claim was wrong." **Stated honestly rather than oversold**: this app's regime history lives in `localStorage` only (`MAX_HISTORY = 400` snapshots, ~5 min apart, only while a tab is open — the still-open server-side-24/7 P0 item below), so this backtests whatever confirmed calls actually accumulated in one browser session — a real, bounded dataset, not a multi-year replay; the UI states "X of Y confirmed calls have a resolvable forward price so far" rather than implying completeness. Runs on-demand (a RUN button in a new BACKTEST panel section), not on every poll tick, since it's one Binance request per horizon per call and re-running it every 5 minutes would multiply that for no benefit — resolved calls don't change. **One honest type fix needed to build this correctly**: `confirmedRegime` was already being persisted into every history entry at runtime (`evaluate()` writes the full `RegimeReading` into history, and structural typing let that slide past the narrower `RegimeSnapshot` type silently) but wasn't declared on `RegimeSnapshot` itself — formalized as an optional field with a comment explaining why it was already there; no behavior change, just an accurate type instead of an implicit `as any` the backtest would otherwise have needed.
- **Signal-family grouping — closes another Strategic Direction P1 item, the "8/10 agree" overstatement the audit flagged** (v9.33): several of the 10 regime signals are correlated readings of the same underlying thing (`fng_level`/`fng_roc` both come off one Fear&Greed index; `oi_roc`/`funding` both come off the same BTC perp market), so a raw "8 of 10 signals agree" count can overstate how independent that agreement really is. New `src/lib/regime/families.ts` groups the 10 signals into 5 genuinely independent families (Trend, Derivatives, Flow, Sentiment, Dominance); `engine.ts` computes each family's own weighted score and internal agreement (judged against that family's own dominant direction, not the overall reading's) and adds a `families: FamilyReading[]` field to `RegimeReading`; `buildReasons()` now includes a line like "That holds across 4 of 5 independent signal families." `RegimePanel.tsx` gets a new 🔗 FAMILIES badge alongside the existing AGREE badge, and both the signal breakdown (under WHY?) and the weight-tuning sliders are now grouped by family instead of one flat list. The AI Council gets this for free — it already ingests `reading.reasons` as plain sentences, no schema change needed on that side.
- **Insider Risk Scanner's "real data" path — the v9.30 finding that was flagged but not fixed — actually fixed, at zero extra request cost** (v9.32): v9.30 found the Etherscan/Birdeye real-data path was dead for every coin (`CoinData` never carried a contract address, so every scan silently fell back to mock data) and deliberately left it unfixed pending a decision on where a per-coin contract address should come from. The README's own suggested fix — an extra CoinGecko `/coins/{id}` lookup per scanned coin — turned out to be unnecessary: the existing bulk `/coins/markets` call already supports `include_platform=true`, which adds a chain→contract-address `platforms` map to every coin in the same request, no extra calls. Flipped on in both places that call CoinGecko (`server/services/scan.ts`'s proxy and `src/services/api.ts`'s direct fallback), threaded through as `CoinData.platforms`, and `insiderRiskApi.ts`'s existing `coin.platforms?.solana`/`.ethereum` reads (already written correctly, just never fed real data) now resolve for real. Native L1 coins (BTC, ETH itself, SOL itself) correctly get an empty `platforms` object and still degrade to mock — that's right, not a remaining gap.
- **AI Council was debating a different "CEO signal" than the one on screen — found during a fresh audit pass over `src/lib/council/`, the area flagged as a next-candidate source** (v9.31): `src/lib/council/context.ts` had its own private `getCeoSignalLabel()` with different thresholds and a completely different label vocabulary (`'STRONG ACCUMULATION SIGNAL'`, `'NO EDGE'`, etc.) than the canonical one in `src/services/signals.ts` that actually populates the CEO signal shown in the live scanner table (`'AGGRESSIVE LONG'`, `'AVOID / SHORT'`, etc.). Two identical coins could get a different CEO signal depending on whether you were reading the scanner or asking the Council about that coin. Fixed by re-exporting the canonical function from `context.ts` instead of maintaining a duplicate — confirmed nothing else imports `getCeoSignalLabel` from `council/context` (only `buildCouncilContext` is consumed externally), so this was safe to collapse.
- **Full repo-wide lint cleanup — 129 pre-existing eslint problems → 0 errors, one genuine Rules-of-Hooks bug found and fixed along the way** (v9.30): George asked for all 129 problems fixed with no artificial scope limit, since there were no real constraints stopping it. All fixes are real typing/logic corrections — no `eslint-disable` comments added anywhere. **Real bug**: `HLOpportunityPanel.tsx` had a `useCallback` (`handleTradeClick`) called *after* a conditional `if (!enabled) return null` — a genuine Rules-of-Hooks violation, since that hook was skipped on some renders and not others. Moved it above the guard. **~70 `any` types replaced with real types**, the bulk of the count: `debugOverlay.ts` (the `window.WebSocket` monkey-patch now uses a proper `PatchedWebSocketCtor` interface instead of five separate `as any` casts), `hyperliquid.ts` (`postTrading()` now returns `unknown` instead of `any` — it's a genuinely-untyped external API response — with real interfaces `HLUniverseAsset`/`HLAssetCtx`/`HLFundingHistoryEntry` cast once at each consumption point instead of `any` leaking through; `hlFetch<T = any>` → `T = unknown`, confirmed safe since every call site already passes an explicit type param), `insiderRiskApi.ts` (generic `getCached<T>()`/`setCached<T>()` instead of an `any`-typed cache map, plus `EtherscanTx`/`BirdeyeTokenOverview`/`BirdeyeHolderRow`/`BirdeyeTransferRow` interfaces — the last one deliberately kept as a wide optional-field type mirroring the code's own existing multi-field-name fallback logic, not fabricated as one pinned shape), `insiderRiskUtils.ts`, `insiderRiskMock.ts`, `pwa.ts` (Background Sync registration, the `installPWA` window global, and iOS's non-standard `navigator.standalone` all properly typed instead of blanket `any`-cast), and every Hyperliquid hook/component that did `markets.forEach((m: any) => ...)` — now typed as the real `HLMarket[]`. **Found and fixed while typing `HLOpportunityPanel.tsx`**: it read `m.indexPrice`, a field that doesn't exist on `HLMarket` and was therefore always `undefined` at runtime (only ever masked by the `any`) — removed; `m.oraclePrice` was already the fallback that always fired anyway, so this is a no-op for behavior, just dead code removed. **Found and fixed**: `NexusCrystalBallV5.tsx` and `WRCrystalBallPro.tsx` are byte-identical 1428-line duplicate files — fixed the same 10 `any`-errors once and copied the corrected file across both (confirmed identical afterward), but the underlying duplication itself is untouched and still worth a decision (e.g. one re-exporting the other). **~15 `no-empty` catch/effect blocks** (`useWhaleWebSocket.ts` ×8, `ArbitrageCommandCenter.tsx` ×3, `useMarketData.ts` ×2, `useWhaleStream.ts`, `cachedFetch.ts` ×2) — all legitimate "this specific failure is expected, fall back to null/treat as already-settled" patterns, not bugs; fixed by adding a real explanatory comment to each rather than leaving it silently empty. **Trivial fixes**: `prefer-const` in `mlScoring.ts`, a `no-useless-escape` regex in `pwa.ts`, and two `no-empty-object-type` interfaces (`command.tsx`/`textarea.tsx`) converted to type aliases. Verified after every batch: `tsc --noEmit -p tsconfig.app.json` clean, `eslint` clean on every touched file, and a final full `npm run build` passes with zero regressions across the whole pass. **33 warnings remain, deliberately untouched pending a decision**: `react-hooks/exhaustive-deps` (adding a missing dependency can change effect timing — real behavioral risk, not a mechanical fix) and `react-refresh/only-export-components` in `src/components/ui/*` (standard shadcn/ui pattern — fixing means splitting boilerplate library files for no functional benefit).

  **Separate real finding surfaced along the way, not fixed — needs a product decision, not made unilaterally**: while typing `analyzeTokenRisk()`/`analyzeBatchRisk()` in `insiderRiskApi.ts`, confirmed the Insider Risk Scanner's "real data" (Etherscan/Birdeye) path is functionally **dead** for every coin this app actually scans. It branches on `coin.chain`/`coin.platforms`/`coin.contract_address`, but the real `CoinData` type (`useMarketData.ts`) never has any of those fields — `coin.id` is a CoinGecko slug like `"bitcoin"`, never a contract address. So `isSolana` is always `false`, `contractAddress` is always `undefined`, both API branches' conditions are always false, `analyzeTokenRisk()` always throws `'No valid API configuration for this token'`, and `WRInsiderRiskScanner.tsx`'s catch block silently falls back to `generateMockInsiderData()` on every single scan — regardless of the `useRealData` toggle or whether Etherscan/Birdeye API keys are configured. This was only visible before because `any` masked the type mismatch entirely. Documented honestly via a new `InsiderRiskCoin` type + inline comment rather than fabricating a fix — a real fix needs deciding where a per-coin contract address should actually come from (e.g. an extra CoinGecko `/coins/{id}` lookup per scanned coin, a new cost/architecture decision). **Fixed in v9.32, see above.**


- **Two P0 items closed off George's external audit of v9.28, one real pre-existing lint bug fixed along the way** (v9.29): (1) fixed the alert-loss cooldown bug the audit found in `useRegimeAlerts.ts` — `setLastAlertedRegime()`/`lastRef.current` were being set unconditionally even when `alertCooldown.checkAndRecord()` returned `allowed:false`, so a cooldown-suppressed regime transition got permanently marked "already alerted" and could go un-notified forever once the cooldown cleared; both now only update inside the `if (allowed)` block, so a blocked tick correctly retries on the next poll instead of being silently swallowed. (2) Real market-wide breadth: `signals.ts`'s breadth signal previously read `up/total` off whatever coins the user happened to be scanning — now `marketBreadthSignal()` pulls Binance's full 24hr ticker, filters to non-leveraged USDT spot pairs, ranks by `quoteVolume`, and computes % green across the top 150 by volume — genuinely market-wide, not watchlist-scoped. Falls back to the old watchlist proxy (clearly labeled as a fallback in `detail`, not silently) if the market-wide fetch fails, rather than losing the signal entirely. Also added a `title` tooltip on the regime score ("composite weighted-signal score, 0-100 — not a probability") per the audit's minor labeling note. **Found and fixed along the way, unrelated to the audit**: `whaleFlowSignal()` in the same file had a pre-existing `@typescript-eslint/no-unused-expressions` lint error (a parenthesized ternary used as a statement in a `for` loop) that predates this session — rewritten as a plain `if`/`else`. Verified `tsc --noEmit -p tsconfig.app.json` zero errors, `eslint` zero errors/warnings on every file touched this session (confirmed a full repo `eslint src` run separately — 129 pre-existing problems remain across unrelated files like `insiderRiskUtils.ts`/`pwa.ts`/`safeInvoke.ts`, mostly `no-explicit-any`, not introduced or touched this session), and a full `npm run build` passes cleanly. **Not attempted this session** (the audit's remaining P0/P1 items): a true server-side 24/7 regime engine + persistent Supabase-side regime history (both need real architecture decisions plus Supabase deploy access this sandbox doesn't have), signal-family grouping, the Early/Developing/Confirmed 3-tier ladder, and backtesting the regime engine's own historical calls.
- **`regimeAlerts.ts` built for real, `useRegimeAlerts` actually wired into `RegimePanel` — closes the gap the previous entry below found** (v9.28): the v9.27 entry below found that v9.26's claimed `regimeAlerts.ts`/`useRegimeAlerts.ts` wiring didn't exist in that checkout. Built now: `src/lib/regime/regimeAlerts.ts` exports `getLastAlertedRegime`/`setLastAlertedRegime` (localStorage-persisted, same try/catch-guarded pattern as `weights.ts`'s `loadWeights`/`saveWeights`), `regimeAlertLevel()` (BEAR/DISTRIBUTION/EARLY BULL/BULL/LATE BULL → `high`, RECOVERY/NEUTRAL → `info` — matches the severity split the v9.26 entry originally described), and `buildRegimeAlertText()` (regime + score + held-snapshot count + top 3 reasons + an explicit "no automatic trade executed — review manually" line, kept in every alert on purpose). Also found and cleaned up a real duplication bug while doing this: two near-identical copies of `useRegimeAlerts.ts` existed (`src/hooks/` and `src/lib/regime/`, both with a stale `// src/hooks/useRegimeAlerts.ts` header comment even in the `lib/regime` copy) — kept the `src/hooks/` one (matches where `useRegimeEngine.ts` already lives) and deleted the orphaned duplicate. `RegimePanel.tsx` now actually calls `useRegimeAlerts(reading, addAlert ?? noop)` — `addAlert` stays optional on `RegimePanelProps` so the panel still renders alert-free for any caller that doesn't have it in scope, but `Index.tsx` does pass it, so this is live on the homepage, not just plumbed. Verified `tsc --noEmit -p tsconfig.app.json` (zero errors — including the two pre-existing `useRegimeAlerts.ts` module-not-found errors flagged in v9.27, now gone since the module they were missing now exists), `eslint`, and a full `npm run build` all pass cleanly.
- **Regime Council desk — first item off Strategic Direction's P1 list closed, but the work underneath it exposed a real documentation/code mismatch worth recording as-is** (v9.27): the AI Council gains a 7th agent, **REGIME DESK** (`regime`, gated into `standard`+`deep`, not `quick`), reading the market-wide Regime Engine block (score/regime/confirmedRegime/heldSnapshots/agreeing-signals/reasons) to answer "what regime are we in" and "what would invalidate this" — distinct from the other six agents, which all read this specific token's own data. Required a real refactor, not just a new prompt: `RegimePanel.tsx` previously owned its `useRegimeEngine()` poll loop internally with no way for the reading to reach the Council, so it's now a controlled component (`reading`/`history`/`weights`/etc. as props) with the hook lifted into `Index.tsx` — the same reading now drives both `RegimePanel` and `WRCouncilPanel`/`buildCouncilContext` off one poll loop instead of two. `supabase/functions/agent-council/index.ts` gets the matching system prompt and a `MARKET REGIME:` line in `buildBrief()` — the server's agent loop is fully generic over `DEPTH_AGENTS`, so no other hardcoded-roster site needed touching. **Found and fixed along the way**: `Index.tsx` was passing an `addAlert` prop to `RegimePanel` that `RegimePanelProps` never declared — a real type error, invisible before because a bare `tsc --noEmit` (no `-p`) wasn't actually checking `tsconfig.app.json`. **Found, not fixed, flagged plainly rather than quietly built around**: the v9.26 entry below claims `regimeAlerts.ts`/`useRegimeAlerts.ts` were built and wired into `RegimePanel` via `addAlert` — in this checkout, `regimeAlerts.ts` doesn't exist anywhere in the repo, two near-identical orphaned copies of `useRegimeAlerts.ts` (`src/hooks/` and `src/lib/regime/`) both import it and fail to compile, and `RegimePanel.tsx` had no `addAlert` usage or `useRegimeAlerts` import at all before this session's edits. Whether that work landed on a branch/session not reflected in this checkout, or the v9.26 entry described work that didn't actually ship, isn't something this session can determine — recorded here rather than silently treated as done. Verified `tsc --noEmit -p tsconfig.app.json` + `eslint` + a full `npm run build` (vite build + postbuild prerender-shells) all pass cleanly for every file this session touched; the two `useRegimeAlerts.ts` module-not-found errors are unrelated and were present before this session's changes. Could not deploy `supabase/functions/agent-council` or `coingecko-proxy` from this session's sandbox — no network path to Supabase — both are code-complete with deploy pending.

- **Regime transitions now reach Telegram/Discord — the first of v9.25's "still genuinely missing" list closed** (v9.26): new `regimeAlerts.ts` + `useRegimeAlerts.ts`, wired into `RegimePanel` via a new `addAlert` prop. Fires only when `confirmedRegime` actually *changes* — never on a raw/unconfirmed fluctuation, since that would defeat the exact false-positive suppression the persistence mechanism (v9.25) exists for. Reuses the app's existing alert pipeline rather than building a parallel one: goes through `alertCooldown.checkAndRecord('regime', level)` (the same circuit-breaker `useMarketData.ts`'s scanner alerts already use, own `'regime'` tag so it can't be suppressed by — or suppress — per-symbol alert activity) and `dispatchNotification()` (the same Telegram/Discord fan-out point, silently no-ops for unconfigured channels). BEAR/DISTRIBUTION and EARLY BULL/BULL/LATE BULL alert at `high` severity — this system treats downside and upside as equally worth surfacing, not just bull signals; RECOVERY/NEUTRAL stay `info`-only, since "nothing to act on yet" isn't the same urgency. The alert text follows the original strategic-direction doc's own example structure — regime + score + persistence count + top contributing reasons + an explicit "no automatic trade executed, review manually" line, kept in every alert on purpose. **Still open from v9.25's list**: the restructured Regime Council desk, the decision-outcome loop ("Mark as Reviewed"/"I Bought"), and real market breadth beyond the current watchlist-scoped proxy.

- **Regime Engine P0: shipped via Lovable, independently of the parallel draft in this doc — audited against source, one real bug fixed** (v9.25): the actual live implementation (`src/lib/regime/{types,signals,engine,weights}.ts`, `useRegimeEngine.ts`, `RegimePanel.tsx`) turned out more complete than the earlier draft this changelog described — worth documenting for real rather than leaving the record pointing at code that was never deployed. **Verified, not assumed**: all 10 signals (BTC trend + momentum via EMA50/200 on real Binance daily klines, market breadth from the live scanner, whale buy/sell $ ratio from the live whale feed, aggressive taker flow from Binance aggTrades, OI rate-of-change + funding from Binance futures, Fear&Greed level + 7d change, BTC dominance trend from CoinGecko) hit real endpoints — checked this specifically because 2 of the 4 external hosts (`fapi.binance.com`, `api.alternative.me`) looked like they might not be covered by `coingecko-proxy`'s CORS allowlist; read the edge function source directly and confirmed all 4 hosts are in fact allowlisted, so this isn't silently failing in production. A missing/failed read scores `null` and drops out of the weighted average rather than being counted as a fake neutral — confirmed this holds for every signal, not just some. Persistence/delta math uses real elapsed time (5m/30m/2h windows off actual timestamps) rather than "N consecutive polls" — a meaningfully more correct design than an earlier draft of this feature had. **Bug found and fixed**: dragging a weight-tuning slider (`⚖ WEIGHTS` in the panel) fired `onChange` per pixel moved, and each call went through `evaluate()` — which unconditionally persists whatever reading it's given. A single slider drag could append dozens of near-duplicate entries (stale signal data, fresh timestamps) into the same history the persistence/confirmation count and delta windows read from, undermining the exact false-positive suppression the persistence mechanism exists for. Split `evaluate()` (used only by the real 5-minute poll tick — computes AND persists) from a new `rescore()` (used by the weight slider — computes against existing history for an instant preview, never writes). **Left as-is, flagged rather than changed unilaterally**: `PERSISTENCE_SNAPSHOTS = 3` at a 5-minute poll interval confirms a regime after 15 minutes — noticeably looser than the original strategic-direction doc's proposed 5min → 30min → 2h persistence ladder. Not touched here since the right value is a judgment call about false-positive tolerance, not a correctness bug; worth a deliberate decision, not a silent change.

- **Protection engine: two more "logic exists, no UI consumer" gaps closed, one architectural dead-end honestly flagged instead of forced** (v9.24): swept `src/lib/nexus/` for the same pattern this project keeps finding. `clearAllLocks()` (`protections.ts`) was fully implemented and completely uncalled — `ProtectionBanner` could only clear locks one at a time, so a MaxDrawdown ALL-PAIRS lock plus several per-pair cooldowns meant several individual clicks; a "Clear All" button now appears whenever more than one lock is active. `getProtectionConfig()`/`resetProtectionConfig()` had the same problem from the other direction: `ProtectionOptimizerPanel` could *apply* an AI-suggested config, one-directional, but nothing anywhere showed what thresholds were actually active right now (shipped defaults? a past optimizer run? hand-edited?) or offered a way back. New `ProtectionConfigPanel` (Portfolio page, collapsed by default — a "check occasionally" reference, not something competing for attention with the locks banner or the optimizer suggestion) shows all four protection checks' live thresholds, flags when they differ from `DEFAULT_PROTECTION_CONFIG`, and wires the reset button that already existed with nowhere to live. **Investigated and deliberately NOT force-fixed**: `getManifest()`/`describeManifest()` (`nexusManifest.ts`) — explicitly built per its own docstring "for AI agents (an MCP client...) deciding whether and how to call a Nexus action," and still has zero callers anywhere. Traced why: every value it reads (`isDryRun()`, `getProtectionConfig()`, `getMaxOpenTrades()`) is `localStorage`-backed, browser-only, per-device state — and this app's actual MCP server (`src/lib/mcp/`) runs as a Supabase Edge Function, which has no path to any specific user's browser storage the way it does to the Binance API or the Supabase DB the other 14 tools read from. Confirmed the in-app AI Council (`src/lib/council/`) doesn't do tool-calling or reference Nexus either, so there's no second consumer hiding elsewhere. Wiring this up for real would mean a genuine architecture change (e.g. the browser pushing bot state to Supabase for the Edge Function to read), not a small gap-close — flagged here rather than either skipped silently or forced into a fix that doesn't actually reach the audience it was written for.

- **Correction: v9.18's `list_council_decisions` exact-match fix never actually shipped — fixed for real now** (v9.23): auditing `src/lib/mcp/` for the next roadmap candidate turned up something more concrete than a new gap — a regression in an already-claimed fix. `src/lib/mcp/tools/list-council-decisions.ts`, the file `index.ts` actually imports and registers as the live `list_council_decisions` tool, still had the original `.ilike("symbol", `%${symbol}%`)` substring-wildcard bug v9.18 said was closed (filtering by `BTC` could still return `SBTC`/`BTCX`/anything containing the letters). The real fix — exact case-insensitive match, `.ilike("symbol", symbol.trim())` — existed in the repo, but in a second, differently-named file (`mcp-list-council-decisions.ts`) that nothing ever imported; a near-identical stray duplicate existed for `binance.ts` too (`mcp-binance.ts`, an older pre-v9.19 snapshot, superseded and also unreferenced — confirmed not hiding a fix, just cruft). Verified this wasn't just a source-tree inconsistency: the committed `supabase/functions/mcp/index.ts` bundle (auto-generated from `src/lib/mcp/` on every `npm run build` — see its own "do not edit" header) has the same substring bug baked in at the same line, so this was live in the actually-deployed tool, not just stale in an editor. Fixed at the real, imported file this time, with the same explanatory comment the orphaned copy had; both dead duplicate files removed so a future pass can't mistake either for the live source again — `git grep` confirms neither was referenced anywhere before deletion. A `git blame`/history dig into exactly how the fix landed in the wrong file wasn't done here — flagging the mechanism as unknown rather than guessing.
- **Solana pre-pump-CEX-transfer detection: the real gap flagged in v9.21 is now wired, not just flagged** (v9.22): `BirdeyeService.analyzeToken()` previously hardcoded `largeTransfers: []`/`cexTransfers24h: 0`/`cexTransfers72h: 0`/`prePumpTransfer: null` for every Solana token, unconditionally — Ethereum tokens got real detection via `EtherscanService`, Solana tokens never did. New `BirdeyeService.getTokenTransfers()` calls Birdeye's `POST /token/v1/transfer` (the wallet-to-wallet transfer feed, not a DEX-trades endpoint — a swap's counterparty is an AMM pool, not a CEX wallet, so no trades endpoint could ever show a "sent to Binance" pattern), filtered server-side to $500k+ transfers to match the Ethereum path's threshold. `analyzeToken()` now maps those into real `TransferEvent`s and runs the same pre-pump logic Ethereum already had. That logic was duplicated between the two services before this — pulled out into a shared `detectPrePumpPattern()` in `insiderRiskUtils.ts` so both chains use one implementation instead of two that could drift. **What's still not done, on purpose**: `SOLANA_CEX_ADDRESSES` (`src/types/insiderRisk.ts`) ships empty. Unlike the Ethereum `DEFAULT_CEX_ADDRESSES` list, there's no checksum or other self-verifying property to a Solana base58 address, and no canonical public source found trustworthy enough to hardcode — a wrong guess here would misattribute an unrelated wallet's activity as a CEX deposit, worse than the previous always-empty state. The plumbing works with zero, one, or many addresses; large-transfer visibility (amount, timing, counterparty) is real starting now, the "isToCEX" tag just won't light up until real addresses are added (`analyzeTokenRisk()`'s `settings.solanaCexAddresses` overrides the default, so this doesn't require editing the repo to populate). **Bonus fix found along the way**: `detectCEX()` (`insiderRiskUtils.ts`) lowercased every address before comparing — correct for Ethereum (mixed case there is just EIP-55 checksum styling, not a distinct character), silently wrong for Solana, where base58 case *is* significant and lowercasing two different addresses could theoretically collapse them into a false match. Now only lowercases `0x...` addresses; Solana addresses compare exact-case.
- **Scan history drill-down: `getScanCoins()` had the same "no UI consumer" gap `WRHistoryPanel` was built to close, independently** (v9.21): the panel's own v9.6-era docstring listed `getScanSessions()`/`getTokenHistory()`/`getTopThreats()` as the working-but-unwired read API it closes — `getScanCoins(sessionId)` (`GET /scans/:id`, already sorted by score server-side) had the exact same problem and was missed at the time. The "RECENT SCAN SESSIONS" list showed a summary row per session (coin count, crit/high count) with no way to see what was actually in it. Session rows are now clickable — expands to that session's real coin list (symbol, threat badge, score), fetched on click, collapses on a second click. Separately looked at (and deliberately left alone): `insiderRiskUtils.ts`'s standalone `analyzePrePumpPattern()`/`fetchEtherscanTransfers()`/`fetchEtherscanTokenHolders()`/`fetchBirdeyeTokenData()` are genuinely dead — but not a wiring gap to close, they're superseded by near-identical private logic inside `insiderRiskApi.ts`'s `EtherscanService`/`BirdeyeService` classes, which *are* live. One real, honestly-self-documented limitation surfaced while checking this: `BirdeyeService.analyzeToken()` hardcodes `largeTransfers: []`/`prePumpTransfer: null` for Solana tokens with an inline comment explaining why (Birdeye needs a separate tx-history call this class doesn't make) — Ethereum tokens get real pre-pump-CEX-transfer detection via `EtherscanService`, Solana tokens don't. Not fixed here: closing it properly needs a verified Solana CEX-address list (the existing `DEFAULT_CEX_ADDRESSES` is Ethereum-only), which isn't something to fabricate — flagged, not guessed at. *(Closed in v9.22 above — the transfer-detection half; the address list itself remains a deliberate manual step.)*
- **Roadmap item closed: real web push notifications, end to end** (v9.20): `src/lib/pwa.ts`'s `subscribeToPush()` had sat unwired since v9.10 for a real reason — it needed a VAPID key pair and a server-side send endpoint, materially bigger than a UI-only fix. That backend now exists: `server/services/pushService.ts` (VAPID signing + `web-push`, `sendToAll()` broadcast, auto-prunes subscriptions the push service reports as gone — 404/410), `server/routes/push.ts` (`/api/push/{vapid-public-key,subscribe,unsubscribe,send}`), a `push_subscriptions` migration (endpoint-keyed, no `user_id` — this app has no login system, so it's a broadcast-to-every-browser model, not per-user targeting), a `push-proxy` Supabase Edge Function (same never-hold-the-token-client-side indirection as `nexus-bot-proxy`), a real `public/sw.js` `push` handler (the OLD `sw.js` was a self-unregistering kill switch — see the file's own history comment for why, and why the rewrite deliberately still has no `fetch` handler), and `src/lib/pushBridge.ts` (the client-side `enablePush()`/`disablePush()`/`sendTestPush()` opt-in flow), wired into `WRSettingsPanel`'s new 🔔 PUSH NOTIFICATIONS group. The one piece `push.ts`'s own comment flagged as a deliberate follow-up — nothing called `/send` automatically — is closed here: a CRITICAL-severity alert now auto-sends a push from `Index.tsx`'s `addAlert`, client-throttled to at most once per 60 seconds so a burst of critical alerts can't fire a dozen pushes in a few seconds (`sendPush()`, generalized out of the former test-only `sendTestPush()` in `pushBridge.ts`). Requires `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` set on the Express server (`npx web-push generate-vapid-keys`) — without them, `isPushConfigured()` is false and the Settings toggle/test button surface a clean 503, not a silent failure. Still unwired, and *not* part of this fix: `triggerBackgroundSync()` in the same file — it registers a Background Sync tag, but `public/sw.js` has no `sync` event listener to act on one, so calling it today would silently do nothing. Different API (retrying a queued offline write on reconnect) with no existing offline-write queue in this app to attach it to — a real gap, but a separate, smaller one than push was, and not invented a use for here just to close it out.
- **MCP tools: guard module generalizes caching + adds a real upstream-load safeguard, cleaned up the redundant bits** (v9.19): v9.18 (below) added a hand-picked `cached()` wrapper around the three heaviest MCP upstream calls. That's since been generalized into a proper shared module — `src/lib/mcp/guard.ts` — which `binanceGet()`/`binanceFuturesGet()`/`jsonFetch()` in `binance.ts` now call internally for *every* request, keyed by the exact URL (or method+url+body for POSTs), with promise-level de-duplication (concurrent identical requests within the same tick share one in-flight fetch, not N). On top of caching, `withBudget()` adds a genuine answer to the audit's "no rate limiting" P0 concern: a global token bucket (sustained + burst) plus a concurrency cap across *all* upstream calls this Edge Function instance makes — this isn't per-client throttling (still not possible; see v9.18's note on why `defineTool` handlers have no caller identity to throttle against), but it does directly address the actual concern raised — a shared upstream call budget that can't be blown past regardless of how many distinct callers are hitting the MCP endpoint. Cleaned up along the way: the three tool files (`get-top-movers.ts`, `search-assets.ts`, `get-hyperliquid-market.ts`) still had v9.18's original manual `cached("binance:ticker24hr", ...)`/`cached("hyperliquid:metaAndAssetCtxs", ...)` wrappers around calls that now cache themselves internally — harmless (correct output either way) but redundant double-caching; removed, since `binanceGet`/`jsonFetch` now handle it automatically for every call, not just these three. Also: `list_council_decisions`'s exact-match fix (see v9.18) had gone missing from this checkout somewhere along the way — re-applied and reverified in the rebuilt bundle.
- **MCP tools: fixed the real BTC/ETH bug, added upstream caching, corrected two indicators, tightened a filter** (v9.18): an external audit of `src/lib/mcp/` found a critical, several real, and a couple of speculative issues — worked through the real ones:
  - **Critical, actually fixed now**: `binance.ts`'s `toPair()` had `/(USDT|USDC|BUSD|BTC|ETH)$/.test(raw)` — which matched `"BTC"` and `"ETH"` against *themselves* (the whole string is the suffix), so a bare `BTC` or `ETH` query hit Binance as `symbol=BTC`/`symbol=ETH` instead of `BTCUSDT`/`ETHUSDT` — Binance has no such pair, so `get_market_snapshot`, `get_whale_trades`, `get_orderbook_pressure`, `get_price_history`, `get_trade_flow`, `get_technical_indicators`, and `get_funding_and_open_interest` all failed on the two most commonly requested assets. A prior attempt at this fix hadn't actually reached `src/lib/mcp/binance.ts` (the real source file) — only the *auto-generated* `supabase/functions/mcp/index.ts` bundle exists, and it explicitly says not to hand-edit it (regenerated from `src/lib/mcp/` by the `@lovable.dev/mcp-js` Vite plugin on every `npm run build` — verified: rebuilding now regenerates it with the fix). The check now requires a non-empty base-asset prefix before the quote suffix, so `BTCUSDT`/`ETHBTC`/`SOLBTC` still pass through unchanged — only a bare quote-currency-lookalike with nothing in front gets `USDT` appended. (This exact regex was later simplified during the v9.19 guard-module refactor to just `/(USDT|USDC|BUSD)$/` — a deliberate tradeoff, not a regression: none of these tools document cross-quoted pairs like `ETHBTC` as valid input, so the simpler check is fine for their actual documented scope.)
  - **Upstream load**: `get_top_movers`, `search_assets` (both fetch Binance's entire 24hr ticker dataset) and `get_hyperliquid_market` (fetches Hyperliquid's entire market universe) originally got a hand-picked 8s cache — since generalized to every upstream call automatically (see v9.19 above).
  - **`get_technical_indicators`**: RSI(14) and ATR(14) were computed with a naive last-N-bars average, not Wilder's smoothing — the audit correctly called this a quant-correctness gap, not a protocol bug. Both now use Wilder's method (seed SMA, then exponential smoothing at α=1/period), matching what TradingView/TA-Lib actually compute for RSI(14)/ATR(14).
  - **`list_council_decisions`**: symbol filter was `.ilike("symbol", "%BTC%")` — a substring match, so filtering by `BTC` could also return `SBTC`/`BTCX`/anything containing the letters. Now an exact case-insensitive match.
  - **`compare_exchange_prices`**: description said "best bid/ask venues" — it actually compares last-trade price, not order-book bid/ask (that's what `get_orderbook_pressure` is for, on a single venue). Reworded to "cheapest/priciest venues"; the response's own `note` field already correctly called out that the spread ignores fees/slippage/withdrawal time.
  - **Not acted on**: the audit's speculation about needing a "2026 MCP protocol" compatibility layer wasn't backed by anything concrete verifiable from this repo — noted here rather than chased on guesswork. Full public/no-auth access (by design — all 14 tools are `readOnlyHint: true`) is unchanged; that's a deliberate tradeoff, not a gap.
- **News feed: CoinDesk and CoinTelegraph were silently failing — only Decrypt ever showed up** (v9.17): `fetchNews()` in `trading-bridge/index.ts` sent a bare `User-Agent: TradingBridge/1.0` with no other headers, and any non-2xx response was silently swallowed (`catch (_) { /* skip dead feed */ }`) — so a feed could fail forever with zero way to diagnose why. Verified independently that both CoinDesk's and CoinTelegraph's RSS feeds are healthy and serving fresh articles; the CDN/bot-protection in front of them was almost certainly rejecting the bare UA the same way Reddit's did (v9.16). Fixed with a fuller, browser-like header set (`NEWS_HEADERS`, mirroring the same fix already applied to the Reddit fetch) and a corrected CoinDesk URL (no trailing slash — matches the URL CoinDesk's own feed self-links to). Failures are now `console.warn`'d with the feed name and status instead of disappearing silently, so a future break is diagnosable from the function logs rather than requiring another round of guesswork. Separately confirmed (not changed, both already healthy): Yahoo Finance's chart endpoint and Binance's public REST API are both working as expected — no fix needed there.
- **Reddit sentiment: real OAuth path, since headers alone couldn't fix it** (v9.16): v9.15's fix (capping error length) confirmed working — but surfaced Reddit's actual message clearly for the first time: `"You've been blocked by network security... use your developer token"`. That's an IP-reputation block on Deno Deploy's datacenter IPs, not a request-shape problem, so no header set was ever going to clear it. `redditSentiment()` now authenticates via Reddit's OAuth `client_credentials` grant when `REDDIT_CLIENT_ID`/`REDDIT_CLIENT_SECRET` are configured as Supabase Edge Function secrets (new `getRedditToken()`, cached until near-expiry) and requests go through `oauth.reddit.com` instead of the anonymous, IP-blocked endpoint — see the new [Reddit Sentiment Setup](#reddit-sentiment-setup) section for how to create a free Reddit "script" app. Without those secrets set, the app still works exactly as before — the panel shows a short, honest "no developer token configured" message instead of Reddit's raw block text. Also fixed: the edge function's error classifier regex didn't match the new `Reddit OAuth ...` failure shapes (token-fetch errors), so those would have fallen through to a raw 500 instead of the intended graceful degrade. **Separately clarified in Deployment Notes**: the reason the v9.15 code fix didn't initially appear live was that Supabase Edge Functions deploy independently of a Lovable/Vercel frontend publish — worth checking which is actually running before assuming a fix failed.
- **Reddit-sentiment failures no longer dump a raw HTML page into the UI** (v9.15): the Trading Hub's Sentiment tab was rendering a multi-KB wall of raw CSS/HTML instead of an error message whenever Reddit's `search.json` endpoint blocked the request (403 — Reddit's public json endpoints have gotten aggressive about non-browser traffic since their 2023 API changes; likely triggered by Deno Deploy's datacenter IPs, not fully fixable from this repo alone). Root cause: `supabase/functions/trading-bridge/index.ts`'s `redditSentiment()` (and, separately, `yahooChart()`) built its thrown `Error` by interpolating the *entire* failed response body — Reddit's block page, in this case — with no length cap, and nothing downstream (`safeInvoke.ts` → `trading-api.ts` → `Sentiment.tsx`'s `String((sent.error as Error).message)`) ever bounded it either, so the whole thing rendered verbatim. Fixed at both ends: new `describeUpstreamError()` in the edge function strips tags and caps every upstream error to ~120 characters before it's ever thrown, and a new `errText()` helper in `trading-api.ts` bounds any error message to 160 characters as a second layer, now used at all 7 Trading Hub call sites that render `.error` (Sentiment, Patterns, Timeframes, Backtest, Dashboard, Screener, Technical) — so this exact failure mode can't recur even from a future endpoint that forgets to truncate. Also tried a genuine (not guaranteed) mitigation: Reddit's request now sends fuller browser-like headers (`Accept`, `Accept-Language`, a more realistic `User-Agent`), since a bare `fetch()` is sometimes enough to trip Reddit's block on its own — if the block is IP-reputation-based instead, this won't clear it; a real fix for that would need Reddit's OAuth API (app registration + credentials), out of scope here. **Bonus fix found along the way**: the edge function's own upstream-vs-generic-error classifier regex required `Yahoo \w+ \d{3}` — `\w+` doesn't match a hyphen, so it silently never matched this app's own default symbol format (`BTC-USD`), meaning Yahoo failures for the app's default symbols always fell through to a raw 500 instead of the intended graceful fallback. Now `[\w-]+`.
- **The app now exposes itself as an MCP server — 14 read-only tools an AI agent can call directly** (v9.14): new `src/lib/mcp/` (via `@lovable.dev/mcp-js`) — see [MCP Tools](#-mcp-tools-ai-agent-access) for the full list. This is a different mechanism from v9.13's crawler-facing static shells: that fix helps a text-only crawler *read* a page, this lets an MCP-aware AI client *call* structured tools (`get_market_snapshot`, `get_whale_trades`, `list_council_decisions`, etc.) and get real JSON back. Verified: fresh `npm install` + `npm run build` (including the v9.13 postbuild step) both pass cleanly against this addition — the two features don't conflict. Small drift fixed along the way: `list_council_decisions`'s description still said the council was "bull/bear/risk/trader/PM", missing the **QUANT** agent added back in v9.8. Also: the prior deploy failure after the v9.13 SEO changes is resolved (fixed on the project side, not diagnosed here — no error log was available to confirm root cause) — a clean reinstall-and-build in a fresh sandbox now succeeds end to end.
- **Text-only AI crawlers (ClaudeBot, GPTBot, etc.) now actually see real per-route content** (v9.13): Whale Radar is a pure client-rendered SPA — `<RouteSeo/>` (`src/components/RouteSeo.tsx`) already updated per-route `<title>`/meta tags on navigation, but only after React mounts, so any crawler reading raw HTML without executing JS (most AI-assistant crawlers, already explicitly allowed in `public/robots.txt`) only ever saw the sitewide `/` title and an empty `<div id="root">` for all 15 routes. New `scripts/prerender-shells.mjs` runs as an npm `postbuild` step: for each route in the new `src/lib/seo/routes.json` (now the single source of truth — `routeMeta.ts` reads the same file instead of hand-duplicating it), it copies `dist/index.html` into a real `dist/<route>/index.html` with that route's actual title/description/canonical/OG tags **and** visible `<h1>`/`<p>` text inside `#root`. Verified safe for real users: `src/main.tsx` uses `createRoot().render()`, not `hydrateRoot()`, so the static shell content just gets cleanly overwritten once JS loads — no hydration-mismatch warnings, no removal script needed, and real users briefly get a themed first paint instead of a blank page. Pure string templating over the one built `index.html` — no headless browser, no new dependencies. **Bonus fix found along the way**: `public/sitemap.xml` only listed 3 of the 15 routes, so most pages were never being surfaced for crawl discovery at all — the same postbuild script now regenerates `sitemap.xml` and `llms.txt` from `routes.json` too, so neither can silently fall out of sync with the real route list again. ⚠ One thing this repo can't verify on its own: whether the deploy host serves these per-route `dist/<route>/index.html` files on a direct request, or SPA-rewrites every path to the top-level `index.html` (which would make the per-route shells unreachable). Verify post-deploy with `curl -s <site>/orderflow | grep '<title>'`.
- **The "✦ AI ASSESSMENT" in Market Sentiment was never actually AI** (v9.12): a different flavor of the gaps above — this one wasn't unreachable, it was a fake. `src/lib/analyzeToken.ts` has had a complete, working `analyzeSentiment()` since early on — same Claude call pattern as the per-coin "AI ANALYZE" button in `WRScanner` (`analyzeToken()`), just summarizing the whole scanned list instead of one token — and it had no caller. Meanwhile `Index.tsx`'s Sentiment modal (`SentimentContent`, opened via the ✦ SENT button) rendered a hardcoded `Market shows {critCount > 3 ? 'ELEVATED' : ...}` template string under an "✦ AI ASSESSMENT" label, and even gated that fake text behind requiring an AI key to be entered — implying a real call was happening when there wasn't one. `SentimentContent` now calls `analyzeSentiment()` for real, with a loading state and a ↻ REGENERATE button; `analyzeSentiment()` itself gained a cache keyed on the actual risk-count signature (crit/high/wash/pump/rug counts + scanned total, not just time) so reopening the modal on an unchanged scan doesn't spend a second API call on a prompt that would come out the same. The three CRITICAL/HIGH/WASH count tiles above it were already real counts and are untouched.
- **Pinning an alert now actually survives a reload** (v9.11): `server/routes/alerts.ts` has had a working `PATCH /api/alerts/:id/pin` endpoint (and a client wrapper, `toggleAlertPin()` in `src/lib/db.ts`) with no caller — `WRRightPanel`'s pin toggle only ever flipped local React state. Root cause: `saveAlert()` discarded the id the server returns on insert, so there was never an id to call `toggleAlertPin()` with, and `loadAlerts()` dropped the id on the read side too. Both now carry it through (`AlertItem.dbId`, populated once the save round-trips or on load from history); `Index.tsx`'s pin handler still flips local state immediately (so the UI never waits on the network) and fires `toggleAlertPin()` alongside it when a `dbId` is available. Alerts saved while offline, or where the save is still in flight, stay local-only until a `dbId` shows up — same honest degrade-to-local pattern the rest of `db.ts` already uses.
- **PWA install prompt and cache clearing are wired up now** (v9.10): `src/lib/pwa.ts` had `setupInstallPrompt()`, `isStandalone()`, and `clearAppCache()` fully implemented with no consumer anywhere in the app — the "Add to Home Screen" flow (browser fires `beforeinstallprompt`, app calls `.prompt()`) and the manual cache-clear-for-a-stuck-old-version escape hatch both existed, unreachable. New `useInstallPrompt()` hook (also in `pwa.ts`) wraps the same `beforeinstallprompt`/`appinstalled` events in a React-friendly form — `setupInstallPrompt()`'s original `window.installPWA` global stays in place for any non-React caller, the hook is just the actual path a component uses. `WRHeader` now shows a **📲 INSTALL** button when the browser offers it (renders nothing on Safari/Firefox, which never fire the event, and nothing once already installed) and `WRSettingsPanel` gets a new **📲 APP** group with install-status text and a **🗑 CLEAR APP CACHE** button. Note: `subscribeToPush()` and `triggerBackgroundSync()` in the same file are left unwired — they need a VAPID key pair and a server-side push-sending endpoint that don't exist yet, a materially bigger backend project than a UI-only fix, so they're called out here rather than half-wired.
- **Scan history is now actually browsable** (v9.9): `src/lib/db.ts` has had a fully working scan-history read API for a while — `getScanSessions()`, `getTokenHistory()`, `getTopThreats()` — backed by real `server/routes/scans.ts` endpoints (`GET /api/scans`, `/api/scans/symbol/:sym`, `/api/scans/threats/top`) and a `scan_coins`/`scan_sessions` table that's been populated on every scan since `saveScan()` was wired into `useMarketData.ts`. Nothing ever called the read side — a "logic existed but never reached the UI" gap, same shape as the v9.6 wallet-tracker and ML-confidence fixes. New `WRHistoryPanel.tsx` closes it: a per-symbol history lookup (score/threat/price across past scans for one token), an all-time top-threats table, and a recent-sessions list. Wired in as a new **📜 HISTORY** tab in `WRRightPanel`, alongside Whale Trades / Wallets / HL — fetched on tab-open and on manual refresh, not polled, since this is backward-looking data that doesn't need a live loop.
- **Roadmap closed: AI Council expanded with a QUANT persona and a structured desk track-record score** (v9.8): the two remaining Roadmap items — additional agent personas, and longer-horizon memory scoring — are both closed. New **QUANT DESK** agent (`quant`, `⌁`→`∑`) reads pure orderflow/derivatives positioning (perp funding rate, OI trend, buy/sell $ imbalance from `recentWhaleTrades`) with an explicit "NO EDGE — insufficient orderflow data" fallback rather than a guessed take; it's gated to `deep` depth only, so `deep` finally has a genuinely different roster from `standard` instead of the same five agents at a higher word limit (`supabase/functions/agent-council/index.ts`'s `DEPTH_AGENTS`). Separately, `buildReflection()`'s longest-horizon grading logic (hits/graded-calls, NEUTRAL excluded) had a structured-data twin added: `computeDeskTrackRecord()` in `src/lib/council/api.ts` produces the same numbers as a `DeskTrackRecord` (hit rate, avg realized return, confidence-discounted 0-100 score pulled toward 50 — the no-information baseline for a binary direction call — when a token has under 5 graded calls) — this existed only as text folded into the PM's prompt before, with no UI consumer; `WRCouncilPanel` now shows it as a 🎯 badge next to the MEMORY button and inside the memory panel. Also fixed along the way: `council-persist`'s `VERDICTS` allow-list (`supabase/functions/council-persist/index.ts`) had drifted to a stale enum (`HOLD`/`WAIT` instead of the real `STRONG_LONG`/`NEUTRAL`/`STRONG_SHORT`) — every decision except plain `LONG`/`SHORT`/`AVOID` was silently rejected at save time, so memory (and therefore the whole track-record feature) never actually accumulated for the majority of verdicts. Confirmed no DB-level `CHECK` constraint on `final_verdict` needed updating alongside it.
- **Smart-money wallet skill scoring** (v9.7): the Roadmap item — "rank tracked wallets by trading skill, not just raw balance/activity" — is closed. New `src/lib/walletSkillScoring.ts` pulls each tracked wallet's last 12 signatures, parses `getTransaction`'s `preBalances`/`postBalances` + `preTokenBalances`/`postTokenBalances` to find unambiguous SOL↔SPL-token swaps (a tx where more than one token balance moves at once is skipped — no honest way to attribute the SOL delta to one mint), then FIFO-matches buys against sells per mint using the same cost-basis convention `server/services/gridPnl.ts` already established for grid fills. Only a *fully* cost-basis-matched sell counts as a scored closed trade — a sell of tokens acquired before the tracked window has no honest cost basis, so it's excluded rather than counted as pure profit, mirroring how `gridPnl.ts` handles a grid's unseeded sell side. Wallets get a confidence-discounted 0-100 skill score (thin sample sizes are pulled toward the middle, same reasoning `pairPerformance.ts`'s score already uses) plus win rate and avg realized SOL profit per closed trade. New `useWalletSkillScoring` hook polls this every 3 minutes, sequentially across wallets with a stagger — separate from the 45s `useWalletActivity` balance poll, since this one makes real `getTransaction` calls and public Solana RPC 429s hard on bursts. `WRRightPanel`'s wallet list now sorts by skill score (unscored wallets stay at the bottom, same "rank, don't hide" convention as `rankByPerformance()`) and shows a 🎯 score / win-rate / avg-SOL badge once a wallet has at least one closed trade.
- **The wallet tracker is actually live now** (v9.6): `WRRightPanel`'s "🐳 WALLETS" tab let you add a Solana address + label since long before this work started, but `WalletEntry.lastActivity` was a field that never got populated — no balance, no activity, just a static labeled list. `src/lib/solanaWallet.ts` now fetches real SOL balance and recent-transaction data via public Solana RPC (no key needed), refreshed every 45s by the new `useWalletActivity` hook. Also: `WRTracker.tsx`'s watchlist bar and this wallet list both benefit from the existing `rankByPerformance()`/real-activity work already in this codebase rather than reading as decoration.
- **Grid maintenance, Volume Maker's trading loop, and real grid PNL — all shipped** (v9.5): `server/services/nexusBotWorker.ts`'s poll loop now does what it previously only stubbed. Grid maintenance: when a level fills (`ccxt.fetchOrder` status check), the opposite order gets re-placed one grid-step away automatically. Volume Maker: `VolumeMakerOpts` gained `exchange`/`symbol` fields (the missing piece that blocked this before), and the worker runs a small, honestly-scoped "ping-pong" tick (`NEXUS_VOLUME_MAKER_TICK_USD`, default $10 — alternating tiny market buy/sell pairs, not a sophisticated market-maker). Grid PNL: `server/services/gridPnl.ts` does FIFO cost-basis matching — sells consume the oldest unmatched buys first — so `GET /grids` reports a real running total instead of a hardcoded 0.
- **Strategy Trader — a second, complementary bot** (v9.4): `server/services/freqtradeClient.ts` bridges to a running freqtrade instance's REST API (`/api/v1/forceentry` etc.), closing the loop from whale-radar's own detection (score + ML confidence + sizing hint) into a real freqtrade position with freqtrade's own stoploss/ROI/protections as a second, independent layer on top of Nexus's own. Same secret-handling pattern as the ccxt bridge — routes through `nexus-bot-proxy`, never a client-held token. A CRITICAL alert's new 🎯 button forwards it manually (never automatically); the server enforces its own cooldown lock and a hard 50%-ML-confidence floor before anything reaches freqtrade. Also new: `mcp-nexus-bot/`, an MCP server wrapping the same `/api/nexus-bot/*` routes so Claude Desktop, Claude Code, or any MCP client can drive Nexus directly — `nexus_scan_arbitrage`, `nexus_create_grid`, `nexus_execute_arbitrage`, and read-only status tools, all through the exact same server-side gates as the browser.
- **A real trading-bot execution bridge** (v9.3) — `registerBot()` previously had no shipped implementation to plug in. `src/lib/nexus/restBridgeBot.ts` now provides one: browser → `nexus-bot-proxy` Edge Function (holds the real server token as a Supabase secret, never client-side) → Express `server/routes/nexusBot.ts` (its own independent dry-run + Postgres-backed cooldown gate, never just trusting the client) → `server/services/ccxtExecutor.ts` (real order placement via [ccxt](https://github.com/ccxt/ccxt) across Binance/OKX/Hyperliquid). Arbitrage execution and grid open/close place real orders end-to-end; grid *maintenance* and Volume Maker's trading loop are explicitly scoped out for now rather than faked — see [Connecting a Trading Bot](#-connecting-a-trading-bot) for exactly what's real today. Also new: `src/lib/nexus/nexusManifest.ts`, a machine-readable manifest of every guarded action and its live gate thresholds, meant to be handed to an LLM-driven caller as context before it acts.
- **Whale-radar side gets its own round of freqtrade concept ports** (v9.2), separate from the earlier Nexus Bot batch: `src/lib/alertCooldown.ts` (StoplossGuard/MaxDrawdownProtection — per-symbol + global circuit breaker on the alert feed itself, not just bot trades), `src/lib/notifyChannels.ts` (Discord webhook + Telegram bot push for alerts that clear the cooldown gate, configurable severity floor), `src/lib/mlScoring.ts` (FreqAI-lite — a from-scratch logistic-regression model trained on this app's own recorded signal outcomes, surfaced in `WRSignalEval`'s "🧠 ML Confidence Model" section with a train/retrain button and honest baseline-vs-high-confidence win-rate comparison), `src/lib/sizingHint.ts` (Edge-Positioning-lite — turns each signal category's real backtested expectancy into a short sizing note shown right on the live alert, not just in the eval panel), and a small multi-exchange abstraction (`src/lib/exchanges/{types,binance,bybit,okx,kraken,registry}.ts` + `src/hooks/useExchangeFeed.ts`) that added OKX and Kraken as live whale-feed sources alongside Binance/Bybit.
- **Three "logic existed but never reached the UI" gaps closed**: `src/lib/nexus/remotePairList.ts` previously fetched and displayed a curated symbol list in Settings but never actually used it to filter anything — it now gates the whale-radar scan for real via a new `remoteWhitelistFilter` in `pairFilters.ts`. The Kraken exchange adapter was fully written but never instantiated anywhere — it's now a live feed source next to OKX. And `pairFilters.ts`'s rejection reasons (why a coin didn't fire an alert) used to go straight to `console.debug` — they're now exposed through the hook and shown as a "🔍 N FILTERED" badge (hover for the reasons) next to the alert feed header.
- **Known remaining gap, called out rather than hidden**: `src/lib/nexus/pairPerformance.ts`'s `rankByPerformance()` export has no consumer yet (nothing re-sorts a symbol list by it), and `mlScoring.ts`'s `predictConfidence()` isn't surfaced per-coin in the live scanner table — only in `WRSignalEval`'s aggregate comparison. Tracked in [Roadmap](#-roadmap).
- **Six more freqtrade-concept ports** (v9.1): `pairPerformance.ts` (PerformanceFilter — ranks tradeable symbols by historical signal outcome instead of just gating them), Sortino/Calmar/SQN added to `backtestMetrics.ts` next to Sharpe/profit-factor (now shown in `WRSignalEval`'s risk table), `openTradesLimit.ts` (FullTradesFilter — blocks new grids once the concurrent-position cap is hit, checked before the risk-based protection gate), `protectionOptimizer.ts` (hyperopt-style local grid-search that suggests protection thresholds from real closed-trade history — never auto-applied), `remotePairList.ts` (RemotePairList — fetch a curated symbol list from an external JSON URL with cache/TTL fallback), and a **dry-run mode** for the Nexus Bot's guarded execution wrappers (`isDryRun()`/`setDryRun()` in `bot.ts`) that runs the full gate chain without placing real orders.
- **UI for all of the above**: `NexusBotStatusBar` (dry-run badge + live open-trade-slot count, mounted on Grid Studio/Volume Maker/Portfolio), `ProtectionOptimizerPanel` (shows the optimizer's top suggestion with an explicit Apply/Dismiss — mounted on Portfolio), and a new **🐋 NEXUS BOT** group in the Settings panel (dry-run toggle, max open trades, remote pairlist URL + manual refresh). Protection thresholds are now persisted (`getProtectionConfig`/`setProtectionConfig` in `protections.ts`) so the optimizer's "Apply" button has something durable to write to instead of the hardcoded default.
- **Protection engine for Nexus Bot** (`src/lib/nexus/protections.ts`, `botTradeStore.ts`) — four risk-control checks (cooldown, stoploss guard, max drawdown, low-profit-pairs) now gate every grid/volume-maker execution before it reaches the connected bot, with real closed-trade outcomes fed back into the ledger and an in-app banner (`ProtectionBanner`) showing active locks with a manual clear. See [Protection Engine](#-protection-engine) below.
- **Fixed white-screen crash on Vercel/Lovable** — `src/integrations/supabase/client.ts` used to call `createClient()` unguarded at module import time; a missing/misnamed `VITE_SUPABASE_*` env var crashed the whole app before React could mount, and no `ErrorBoundary` could catch it (import-time errors aren't render errors). It now falls back to inert placeholder credentials and accepts either `VITE_SUPABASE_PUBLISHABLE_KEY` or `VITE_SUPABASE_ANON_KEY`, so a missing key just disables Supabase-backed features instead of blanking the whole app.
- **Debug/startup diagnostics** (`src/lib/startupDiagnostics.ts`, `src/lib/debugOverlay.ts`) — installed before any other import in `main.tsx` specifically to surface bootstrap failures instead of a silent blank page.
- **Chunk-recovery** (`src/lib/chunkRecovery.ts`) — auto-reloads the app if a lazy-loaded chunk fails to fetch (stale deploy after a new build goes live).
- **`safeInvoke` wrapper** for all Supabase Edge Function calls — pre-flight config check + try/catch so a bad/localhost Supabase URL in production degrades gracefully instead of throwing.
- **Hyperliquid data pipeline moved server-side** — all Hypurrscan calls now go through a Supabase Edge Function (`hyperliquid-cache`) with a Postgres-backed cache + rate counter, instead of calling `api.hypurrscan.io` directly from the browser (see `HYPERLIQUID_DEPLOY.md`).
- **SEO overhaul** — full Open Graph / Twitter Card / JSON-LD structured data, canonical URL, PWA manifest, and a strict `Content-Security-Policy` meta tag whitelisting every external host the app talks to (Binance, Bybit, Supabase, CoinGecko, DexScreener, Birdeye, Etherscan, Helius, Hyperliquid, Backpack, and CORS proxies).
- **21+ confirmed bugs fixed** across multiple systematic audit passes (real-data pipeline correctness, Hyperliquid perps integration, WebSocket reconnect logic).

---

## 📊 Core Features

- **Live market ticker** — real-time prices/volumes across tracked assets.
- **Whale transaction scanner** (`WRScanner`, `useWhaleStream`, `useWhaleWebSocket`, `useExchangeFeed`) — streams large trades from Binance and Bybit WebSockets (hardened, hand-tuned reconnect/circuit-breaker logic), plus OKX and Kraken via a generic adapter-driven hook (`src/lib/exchanges/`) — with an HTTP-seed fallback and exponential backoff + jitter reconnect if a socket drops.
- **Advanced filters** (`WRAdvancedFilters`) — filter the whale feed by exchange, size, side, symbol, etc.
- **Alert quality & delivery** (`src/lib/pairFilters.ts`, `alertCooldown.ts`, `notifyChannels.ts`, `sizingHint.ts`) — freqtrade-pattern pre-filters (dead pairs, implausible ticks, illiquid DEX pools, poor-track-record symbols, remote whitelist) keep noise out before an alert ever fires; a StoplossGuard/MaxDrawdown-style cooldown throttles what's left; alerts that clear both gates optionally push to Discord/Telegram and carry a real backtested-expectancy sizing note.
- **Stats bar, tracker & portfolio** (`WRStatsBar`, `WRTracker`) — running session stats and a watchlist/portfolio with local persistence, ordered by historical performance (`rankByPerformance()`).
- **Solana wallet tracker** (`WRRightPanel`'s 🐳 WALLETS tab, `src/lib/solanaWallet.ts`, `useWalletActivity`) — add any Solana address + a label; real SOL balance and recent-transaction activity refresh every 45s via public Solana RPC, no key required.
- **Alerts** (`WRAlertBell`) — configurable alert conditions with in-app notification bell.
- **Signal evaluation** (`WRSignalEval`, `src/lib/signalStore.ts`, `backtestMetrics.ts`, `mlScoring.ts`) — stores emitted signals, fills in realized outcome prices, and reports win-rate/profit-factor/Sharpe/Sortino/Calmar/SQN plus an optional FreqAI-lite confidence model trained on that same history.
- **Onboarding & keyboard shortcuts** (`WROnboarding`, `WRKeyboardHelp`) — first-run walkthrough and a shortcuts cheat-sheet.
- **Mobile filter sheet & responsive layout** (`WRMobileFilterSheet`, `use-mobile`) — full mobile-first UI, not just a scaled-down desktop view.
- **CoinGecko status indicator** (`WRCoinGeckoStatus`) — shows live/degraded/rate-limited state of the price-data source.
- **Insider-risk scanner** (`WRInsiderRiskScanner`, `WRInsiderRiskSettings`, `WRInsiderProgress`) — screens tokens for insider-trading-style risk signals with configurable thresholds.
- **Settings panel** (`WRSettingsPanel`) — runtime configuration, including optional per-browser Supabase URL/key override (no rebuild needed to point at your own project).

## 🐋 Nexus Suite (`/nexus/*`)

- **Whale Watch** — accumulation/distribution visualization.
- **Arbitrage** (`useNexusMarkets`, `src/lib/nexus/arbitrage.ts`) — cross-exchange arbitrage opportunity scanner over live aggregated market data.
- **Crystal Ball** (`NexusCrystalBallV5`, `WRCrystalBallPro`) — predictive trend/probability modeling.
- **Grid Studio** — grid-trading strategy builder.
- **Volume Maker** — synthetic volume/market-making strategy tooling.
- **Portfolio** — P&L and risk tracking.
- **Nexus Bot** (`useNexusBot`, `src/lib/nexus/bot.ts`) — connects a real execution backend via `RestBridgeBot` (see [Connecting a Trading Bot](#-connecting-a-trading-bot)) and reports connection state in the UI. Grid and Volume Maker executions go through the [protection engine](#-protection-engine) rather than calling the bot directly.

## 🛡️ Protection Engine

Concept ported from freqtrade's `plugins/protections/*` and re-implemented from scratch in TypeScript against this app's own bot trade ledger — nothing here places trades, it only decides whether the bot is allowed to open new exposure right now.

- **Bot trade ledger** (`src/lib/nexus/botTradeStore.ts`) — a `localStorage`-backed record of every trade the bot actually closed (grid session, volume-maker run). A trade is only ever recorded after a real close with a known PnL; nothing synthetic is logged.
- **Four checks** (`src/lib/nexus/protections.ts`), each independently configurable:
  - **Cooldown** — blocks new entries on a pair right after it closes.
  - **Stoploss Guard** — too many stopped-out/error closes in a lookback window → lock.
  - **Max Drawdown** — equity-curve drawdown across recent trades exceeds a threshold → lock all pairs.
  - **Low Profit Pairs** — a pair's net PnL over a lookback window is negative → lock that pair.
- **`canTrade(pair, side)`** — the single gate every guarded action calls; locks persist across reloads and expire on their own schedule.
- **Guarded execution wrappers** in `bot.ts` (`executeArbitrageGuarded`, `createGridGuarded`, `startVolumeMakerGuarded`) replace the raw `TradingBot` calls in Grid Studio and Volume Maker; a blocked attempt throws `ProtectionBlockedError` with the human-readable reason, surfaced via the existing toast error handling.
- **Real outcome reporting** — Grid Studio detects an `active → stopped/error` transition and reports `closeProfit = pnl / totalInvestment` (both real, bot-reported values); Volume Maker reports the fee/rebate margin (`(rebatesUsd - feesUsd) / totalVolumeUsd`) when a run stops. No fabricated numbers feed the ledger.
- **`ProtectionBanner`** (`src/components/nexus/ProtectionBanner.tsx`) — renders nothing when there are no active locks; otherwise shows each locked scope, the reason, time remaining, and a manual "Clear" action. Mounted on Grid Studio, Volume Maker, and Portfolio.

## 🔌 Connecting a Trading Bot

`registerBot()` (`src/lib/nexus/bot.ts`) accepts anything implementing the `TradingBot` interface — the app ships one real implementation, `RestBridgeBot`, rather than hardcoding a specific exchange or bot product into the frontend.

**Why the browser can't execute trades directly:** real exchange API keys must never reach client JS — authenticated exchange endpoints generally block browser CORS anyway, and shipping a secret to every visitor's tab is a straightforward compromise. Execution has to happen server-side.

**Architecture:**

```
Browser (RestBridgeBot)
  → supabase.functions.invoke('nexus-bot-proxy')   — browser only ever holds the public anon key
    → Edge Function (supabase/functions/nexus-bot-proxy)
        holds API_AUTH_TOKEN as a Supabase secret, never in client code
      → Express server  POST/GET/DELETE /api/nexus-bot/*  (server/routes/nexusBot.ts)
          re-checks dry-run + a Postgres-backed cooldown independently —
          never trusts that the browser's own gate already passed
        → server/services/ccxtExecutor.ts — real order placement via ccxt
            (binance, okx, hyperliquid; backpack once ccxt supports it)
```

This mirrors the same "browser → edge function → real secret held server-side" pattern already used for `coingecko-proxy`, `hyperliquid-cache`, and `whale-stream` — nothing new architecturally, just applied to order execution instead of market data.

**Setup:**
1. `npm install` inside `server/` (adds `ccxt`).
2. `psql $DATABASE_URL -f server/migrations/002_nexus_bot.sql`.
3. In `server/.env` (never committed): exchange credentials per exchange you want live (`BINANCE_API_KEY`/`BINANCE_API_SECRET`, `OKX_API_KEY`/`OKX_API_SECRET`/`OKX_API_PASSPHRASE`, ...). Only configured exchanges are usable — others fail with a clear error, not a silent no-op.
4. `supabase secrets set NEXUS_BOT_API_URL=https://your-express-server.example.com API_AUTH_TOKEN=<same value as server/.env>`, then `supabase functions deploy nexus-bot-proxy`.
5. In the app, Settings → 🐋 NEXUS BOT → **CONNECT BOT**.

**Dry-run is on by default, in two independent places.** The client-side toggle in Settings only affects the browser's own gate chain; the server enforces its own `NEXUS_DRY_RUN` flag regardless of what the client sends, and only turns off when **both** `NEXUS_DRY_RUN=false` **and** `NEXUS_LIVE_TRADING_CONFIRM=I_UNDERSTAND_THE_RISK` are set — a deliberately exact phrase, not a plain boolean, so it can't flip on from a stray typo.

**Scope, stated plainly:** arbitrage execution and grid open/close place real orders end-to-end. Grid *maintenance* (noticing a level filled and re-placing the opposite order one step away) and Volume Maker's trading loop are both implemented — `server/services/nexusBotWorker.ts` polls every `NEXUS_GRID_POLL_MS` (default 30s) and drives both. Grid PNL is real too: `server/services/gridPnl.ts` does FIFO cost-basis matching (sells consume the oldest unmatched buys first) rather than reporting a hardcoded 0. Volume Maker is deliberately a small "ping-pong" reference loop (`NEXUS_VOLUME_MAKER_TICK_USD`, default $10 — alternating tiny market buy/sell pairs), not a sophisticated market-maker.

**For AI agents:** `src/lib/nexus/nexusManifest.ts` exports `getManifest()` — a structured, machine-readable description of every guarded action, its exact gate thresholds (read live from `protections.ts`/`openTradesLimit.ts`, not hardcoded text), and its input/output shape. Hand this to an LLM-driven caller as context before it acts, the same way a human would read this section first.

**Or let an AI agent drive it directly:** `mcp-nexus-bot/` is an MCP server wrapping the same `/api/nexus-bot/*` routes — point Claude Desktop, Claude Code, or any other MCP client at it and it gets `nexus_scan_arbitrage`, `nexus_create_grid`, `nexus_execute_arbitrage`, and read-only status tools, all going through the exact same server-side gates as the browser. See [`mcp-nexus-bot/README.md`](mcp-nexus-bot/README.md) for setup.

### Strategy Trader — a second, complementary bot

`RestBridgeBot` (ccxt) covers what freqtrade genuinely isn't built for — cross-exchange arbitrage, grid trading. `server/services/freqtradeClient.ts` covers the reverse: single-exchange, strategy-signal-driven entries with freqtrade's own mature stoploss/ROI/protections engine as a **second, independent** layer of risk management on top of anything Nexus already does.

This closes the loop: whale-radar detects a signal (score + `mlScoring.ts` confidence + `sizingHint.ts` expectancy) → a human clicks **🎯 Send to Strategy Trader** on a CRITICAL alert (never automatic) → the same `nexus-bot-proxy` Edge Function forwards to `server/routes/strategyTrader.ts` → freqtrade's `/api/v1/forceentry`, gated by the server's own cooldown lock and a hard 50%-ML-confidence floor, independent of whatever freqtrade's own config decides.

**Setup:** requires a freqtrade instance already running with `api_server.enabled: true`. On the Express server (`server/.env`): `FREQTRADE_API_URL`, `FREQTRADE_API_USERNAME`, `FREQTRADE_API_PASSWORD` (from freqtrade's own `config.json`). Check connection status in Settings → 🎯 STRATEGY TRADER.

**Honesty note:** `server/services/freqtradeClient.ts` is written against freqtrade's documented REST API shape, not verified against a live instance in this environment — confirm each endpoint against your freqtrade version's own `/docs` (Swagger UI) before trusting it with a funded account.

## 🧠 Trading Hub (`/trading-hub/*`)

Dashboard, Screener, Technical Analysis, Patterns, Sentiment, Backtest, and Timeframes (1m–1W) pages under a shared layout (`TradingHubLayout`), with a technical-context badge component shared across views.

## ⚡ Hyperliquid Module

- **Explorer** (`HLExplorer`, `HLBlockTable`) — blocks/txs/wallets browsing.
- **Manipulation Scanner** (`useHLManipulationScanner`, `HLManipulationScanner`) — flags spoofing/layering-style patterns on Hyperliquid perps.
- **Opportunity Panel** (`useHLOpportunities`, `HLOpportunityPanel`) — 8 detection strategies: funding-rate arbitrage, basis trade, long/short squeeze, whale impact, and more, exposed both as a dedicated panel and indexed by symbol so the main scanner can flag matching rows.
- **Wallet Tracker** (`HLWalletTracker`) — watch specific on-chain wallets.
- **Config banner** (`HLConfigBanner`) — warns in-app if Hyperliquid/Supabase config is incomplete instead of failing silently.
- Server-cached via Supabase Edge Function with a 500ms TTL Postgres cache + rate counter (see `HYPERLIQUID_DEPLOY.md`).

## 🏛️ AI Council

`WRCouncilPanel` + `src/lib/council/` — a multi-agent "council" that debates a symbol and produces a final verdict + conviction score. `quick` runs three agents (Bull, Bear, PM); `standard` adds **REGIME DESK** (reads the live market-wide Regime Engine block — score/confirmed regime/held snapshots/agreeing signals — and states what would invalidate the current read) plus Risk Desk and Trader; `deep` adds a seventh, **QUANT DESK**, reading pure orderflow/derivatives positioning (funding rate, OI trend, whale buy/sell imbalance). Decisions are persisted to Supabase (`council_decisions` table) with a running memory of past calls; performance is back-filled against live price at 1h/4h/24h/7d/30d checkpoints, and a confidence-discounted 0-100 desk track-record score (`computeDeskTrackRecord()`, graded at each call's longest available horizon) is shown right in the panel next to the memory list.

## 🔌 MCP Tools (AI Agent Access)

`src/lib/mcp/` — the app exposes itself as an [MCP](https://modelcontextprotocol.io) server via `@lovable.dev/mcp-js`'s `defineMcp`/`defineTool`: a Lovable build-time framework adapter turns `src/lib/mcp/index.ts`'s tool list into real HTTP route(s) automatically — there's no explicit import to wire up elsewhere in the app, the file's location under `src/lib/mcp/` is the convention. This is a different mechanism from the [postbuild static shells](#-whats-new) (v9.13): that fix helps text-only crawlers *read* the site; this lets an MCP-aware AI client (Claude, or any other MCP client) *call* it as structured tools and get JSON back, no page-reading involved. All 14 tools are `readOnlyHint: true` — nothing here places trades or writes data:

| Tool | Source | What it returns |
|---|---|---|
| `search_assets` | Binance | Tickers matching a name fragment — the "what can I even ask about" entry point |
| `get_market_snapshot` | Binance | 24h price/change/high/low/volume for one asset |
| `get_price_history` | Binance | OHLCV candles, 1m–1w intervals |
| `get_top_movers` | Binance | Gainers/losers/volume leaders, liquidity-filtered |
| `compare_exchange_prices` | Binance, OKX, Bybit, Kraken, Hyperliquid | Same asset across 5 venues + spread (a lead, not a verified arb) |
| `get_whale_trades` | Binance | Large aggregated trades above a USD threshold |
| `get_trade_flow` | Binance | Aggressive buy vs sell volume split and bias over recent trades |
| `get_orderbook_pressure` | Binance | Bid/ask liquidity imbalance near the spread |
| `get_funding_and_open_interest` | Binance Futures | Funding rate, mark/index price, OI, top-trader long/short ratio |
| `get_hyperliquid_market` | Hyperliquid | Perp mark price, funding, OI, max leverage — one asset or top markets by volume |
| `get_hyperliquid_wallet` | Hyperliquid | Public wallet snapshot: account value, every open position, liquidation price |
| `get_technical_indicators` | Binance (computed) | RSI(14), SMA(20/50), EMA(12/26), MACD, ATR(14), trend read |
| `get_market_sentiment` | alternative.me, CoinGecko | Fear & Greed index + history, total market cap/volume, BTC/ETH dominance |
| `list_council_decisions` | This app's Supabase | Recent [AI Council](#-ai-council) verdicts, optionally filtered by symbol |

`src/lib/mcp/binance.ts` and `supabase.ts` are the shared fetch helpers (10s timeout + abort, and env-var resolution across `SUPABASE_URL`/`VITE_SUPABASE_URL` naming, matching the rest of the app's Supabase config tolerance). Every `binanceGet`/`binanceFuturesGet`/`jsonFetch` call is cached and budget-limited via `src/lib/mcp/guard.ts` — see [What's New](#-whats-new) v9.19 for what that covers (and doesn't: no per-client throttling, since tool handlers don't receive caller identity). `list_council_decisions` reads with the `anon` key only — RLS-gated, no write access. `supabase/functions/mcp/index.ts` is an **auto-generated bundle** (built by the `@lovable.dev/mcp-js` Vite plugin from `src/lib/mcp/` on every `npm run build`) — never hand-edit it, edit the source files under `src/lib/mcp/` and rebuild.

## 📡 Order Flow Scanner (`/orderflow`)

`useOrderflowEngine` streams Binance spot orderbook depth (`@depth20@100ms`), aggregated trades, and futures liquidations (`@forceOrder`) directly over WebSocket for live bid/ask imbalance and large-block-trade visualization.

## 🛡️ Reliability & Resilience Engineering

This app has been through multiple hardening passes — worth calling out because it's most of what separates v9 from a typical dashboard demo:

- **Error boundaries** wrapped around every route individually, so one broken page doesn't blank the whole app.
- **Startup diagnostics + debug overlay** installed before any other code runs, to catch and surface bootstrap-time failures.
- **Chunk recovery** — reloads automatically on a stale/failed lazy-chunk fetch after a redeploy.
- **`safeInvoke`** — every Supabase Edge Function call is pre-checked and try/caught so misconfiguration degrades gracefully.
- **`db.ts` offline fallback** — all persistence (portfolio, tracked tokens, alerts) falls back to `localStorage` with retry/backoff + toast notifications if the backend API is unreachable, so the app stays usable offline.
- **Rate-limit handling** (`src/lib/rateLimit.ts`) — tracks and surfaces active cooldowns per data source instead of silently failing requests.
- **Network resilience helpers** (`src/lib/networkResilience.ts`, `src/lib/cachedFetch.ts`) — shared retry/backoff/caching layer used across data hooks.
- **Perf budget monitor** (`src/lib/perfBudget.ts`) — tracks runtime performance budgets.
- **Strict CSP** in `index.html` whitelisting only the exact external hosts the app needs.

## 📲 PWA & Performance

- Installable PWA via `manifest.json` + service worker (`registerServiceWorker`), with offline no-JS fallback messaging.
- Real web push notifications (VAPID, RFC 8030/8291) — see [What's New](#-whats-new) v9.20 for the full path from browser subscribe to server broadcast. `src/lib/pushBridge.ts`'s `enablePush()`/`disablePush()`/`sendTestPush()` are wired into `WRSettingsPanel`'s 🔔 PUSH NOTIFICATIONS group; a CRITICAL-severity alert auto-sends a push (client-throttled to at most once/60s) via the same `sendPush()`.
- Manual Vite chunk-splitting (vendor / charts / query / full Radix UI bundle) for faster first paint.
- `esnext` build target, dev-only sourcemaps.

## 🖥️ Backend (optional)

A companion Express + TypeScript API in `server/` (deployed separately, e.g. Railway — see `Procfile`):

| Route | Purpose |
|---|---|
| `GET /api/health`, `/api/health/db` | Liveness + DB connectivity checks (public) |
| `/api/scan` | Scan endpoint (public) |
| `/api/scans` | Scan history |
| `/api/portfolio` | Portfolio persistence |
| `/api/tracked` | Tracked-token watchlist persistence |
| `/api/alerts` | Alert persistence |
| `/api/whale-events` | Whale event log |
| `/api/signal-outcomes` | Signal outcome backfill (`npm run fill-prices`) |

All non-public routes require a shared bearer token (`API_AUTH_TOKEN` on the server, `VITE_API_TOKEN` on the client — they must match). Postgres schema lives in `server/schema.sql` (`npm run db:migrate`).

---

## 🛠️ Tech Stack

| Category | Technology |
|---|---|
| Frontend | React 18 + TypeScript, Vite 5 (SWC) |
| Styling | Tailwind CSS + shadcn/ui (Radix primitives) |
| Data/State | TanStack React Query, custom hooks |
| Realtime | Native WebSocket (Binance, Bybit), Supabase Edge Functions |
| Backend (optional) | Express + TypeScript, PostgreSQL |
| Backend-as-a-Service | Supabase (Postgres + Edge Functions + Auth client) |
| Charts | Recharts |
| Forms/Validation | react-hook-form + Zod |
| Testing | Vitest, Testing Library, Playwright |
| PWA | Workbox-style manual service worker |
| AI Agent Access | MCP server via `@lovable.dev/mcp-js` (see [MCP Tools](#-mcp-tools-ai-agent-access)) |
| Deployment | Vercel, Lovable, Railway/Fly.io (backend) |

---

## 📁 Project Structure

```
crypto-whale-watch-nexus-main/
├── server/                     # Optional Express + Postgres API (Procfile deploy)
├── public/                     # manifest.json, favicons, robots.txt, sitemap.xml
├── src/
│   ├── assets/
│   ├── components/
│   │   ├── whale-radar/        # WR* — core dashboard UI
│   │   ├── nexus/               # Nexus suite UI + ProtectionBanner.tsx
│   │   ├── hyperliquid/         # HL* — Hyperliquid module UI
│   │   ├── trading/             # Trading Hub shared UI
│   │   └── ui/                  # shadcn/ui primitives
│   ├── hooks/                   # useMarketData, useWhaleStream/WebSocket, useExchangeFeed,
│   │                             # useHyperliquid, useHLManipulationScanner,
│   │                             # useHLOpportunities, useOrderflowEngine,
│   │                             # useNexusBot, useNexusMarkets, useProtections, ...
│   ├── integrations/supabase/   # Generated Supabase client + DB types
│   ├── lib/
│   │   ├── exchanges/            # OKX/Kraken/Binance/Bybit adapters (whale-feed WS) + registry
│   │   ├── council/              # AI council engine + persistence
│   │   ├── mcp/                   # MCP server tools (defineMcp/defineTool) — see MCP Tools
│   │   ├── seo/                   # routes.json (single source of truth) + routeMeta.ts
│   │   ├── nexus/                # Arbitrage engine, bot connector + guarded execution,
│   │   │                         # protections.ts (risk engine), botTradeStore.ts (ledger),
│   │   │                         # pairPerformance.ts, openTradesLimit.ts, protectionOptimizer.ts,
│   │   │                         # remotePairList.ts, pairQuality.ts, exchanges.ts (aggregate market data)
│   │   ├── pairFilters.ts, alertCooldown.ts, notifyChannels.ts, sizingHint.ts, mlScoring.ts,
│   │   │                         # backtestMetrics.ts, signalStore.ts — whale-radar alert pipeline
│   │   ├── db.ts                 # Frontend persistence client (offline fallback)
│   │   ├── safeInvoke.ts         # Guarded Supabase Edge Function calls
│   │   ├── supabase.ts           # Lazy, override-friendly Supabase client
│   │   ├── startupDiagnostics.ts, debugOverlay.ts, chunkRecovery.ts
│   │   ├── rateLimit.ts, networkResilience.ts, cachedFetch.ts, perfBudget.ts
│   │   └── detection.ts, analyzeToken.ts, insiderRisk*.ts, ...
│   ├── pages/
│   │   ├── Index.tsx             # Main dashboard ("/")
│   │   ├── Orderflow.tsx
│   │   ├── nexus/                # 6 Nexus pages
│   │   └── trading-hub/          # 7 Trading Hub pages
│   ├── services/                 # api.ts, signals.ts
│   ├── test/
│   ├── App.tsx, main.tsx
├── scripts/prerender-shells.mjs  # postbuild: per-route static HTML shells + sitemap.xml/llms.txt
├── HYPERLIQUID_DEPLOY.md         # Edge Function + DB migration deploy guide
├── Procfile                      # Backend deploy (Railway-style)
├── vite.config.ts
└── package.json
```

---

## 🚀 Getting Started

```bash
git clone <your-repo-url>
cd crypto-whale-watch-nexus-main
npm install
npm run dev            # frontend only, http://localhost:8080
npm run dev:all        # frontend + local Express API together
```

Build for production:

```bash
npm run build
```

Run tests:

```bash
npm run test           # or: npm run test:watch
```

---

## 🔐 Environment Variables

| Variable | Required? | Purpose |
|---|---|---|
| `VITE_SUPABASE_URL` | Optional | Supabase project URL. Missing → Supabase features disabled, rest of app still works. |
| `VITE_SUPABASE_PUBLISHABLE_KEY` or `VITE_SUPABASE_ANON_KEY` | Optional | Supabase anon/publishable key (either name works). |
| `API_AUTH_TOKEN` | Only if self-hosting `server/` | Bearer token required by protected `/api/*` routes. |
| `VITE_API_TOKEN` | Only if self-hosting `server/` | Must exactly match `API_AUTH_TOKEN`. |
| `REDDIT_CLIENT_ID` | Optional (Supabase Edge Function secret) | Reddit "script" app client id — see [Reddit Sentiment Setup](#reddit-sentiment-setup) below. Without it, `/trading-hub/sentiment`'s Reddit panel fails with an actionable "no developer token configured" message instead of crashing. |
| `REDDIT_CLIENT_SECRET` | Optional (Supabase Edge Function secret) | Paired with `REDDIT_CLIENT_ID` above. |

In **Lovable**, set these under Project Settings → Environment Variables (not `.env.local`). In **Vercel**, set them under Project Settings → Environment Variables and trigger a redeploy. Supabase URL/key can also be overridden at runtime per-browser from the in-app Settings panel — no rebuild required. `REDDIT_CLIENT_ID`/`REDDIT_CLIENT_SECRET` are **Supabase Edge Function secrets**, not frontend env vars — set them via `supabase secrets set REDDIT_CLIENT_ID=... REDDIT_CLIENT_SECRET=...` (or the Lovable/Supabase dashboard's Edge Function secrets UI), then redeploy the `trading-bridge` function specifically — a frontend-only publish does not pick these up.

#### Reddit Sentiment Setup

Reddit blocks anonymous (unauthenticated) requests to its `www.reddit.com/*.json` endpoints from datacenter/edge-function IPs outright — its own error is `"You've been blocked by network security... use your developer token"`. No request header fixes this; it needs real OAuth. To enable it:

1. Go to <https://www.reddit.com/prefs/apps> → **create app** → type **script**.
2. Copy the generated **client id** (under the app name) and **secret**.
3. Set them as Supabase Edge Function secrets: `REDDIT_CLIENT_ID`, `REDDIT_CLIENT_SECRET` (see above).
4. Redeploy the `trading-bridge` edge function.

`supabase/functions/trading-bridge/index.ts`'s `getRedditToken()` then authenticates via Reddit's `client_credentials` grant (no end-user login involved, read-only public data) and requests go through `oauth.reddit.com` instead of the anonymous endpoint. Without these secrets set, the app still works — the Reddit panel just shows a short "no developer token configured" message instead of trying (and getting blocked).

---

## 🚢 Deployment Notes

- Frontend: Vercel or Lovable, static Vite build.
- Backend (`server/`): Railway/Fly.io via `Procfile` (`npm run build:server && node server/dist/index.js`).
- Hyperliquid data: deploy the `hyperliquid-cache` Supabase Edge Function + run the DB migration — see `HYPERLIQUID_DEPLOY.md`.
- If the deployed site ever shows a **blank page**: open DevTools Console first. As of this version, `src/integrations/supabase/client.ts` no longer crashes on missing env vars, so a blank screen now points to a genuine runtime error rather than a silent import-time crash — the console will show it.
- If a build **fails to deploy** right after touching anything in [Connecting a Trading Bot](#-connecting-a-trading-bot): `WRSettingsPanel.tsx` and `WRRightPanel.tsx` import `src/lib/nexus/bot.ts`, `restBridgeBot.ts`, and `strategyTraderBridge.ts` directly — a partial upload (some of those files updated, others not) fails the build with a "Cannot resolve module" or type error, not a runtime crash. Check the actual build log for the missing/mismatched module name before assuming the newest file is at fault; it's usually one of the three not being in sync with it.
- **Supabase Edge Functions (`supabase/functions/*`) deploy separately from the frontend.** A Lovable/Vercel "publish" rebuilds and redeploys the Vite frontend — it does not automatically redeploy edge functions. If you've changed a file under `supabase/functions/` (e.g. `trading-bridge`) and the live behavior still looks like the old code, that's the most likely reason: the function itself needs its own deploy — `supabase functions deploy <name> --project-ref <ref>`, or whatever equivalent trigger your platform provides. Check which version is actually live before assuming a code fix didn't work.

---

## 🧭 Strategic Direction (P0 shipped as of v9.25 — see below)

A deliberate reframing of what this project is *for* — recorded here so it governs prioritization from this point on, and so the reasoning survives past the session that decided it.

**Old framing:** a trading bot that executes (Nexus grid/arbitrage/volume-maker, Strategy Trader/freqtrade bridge).
**New framing:** a **personal 24/7 crypto early-warning system** — detects that market conditions are *starting* to shift, explains why, and hands the decision to a human. No automatic execution of the resulting insight; that stays manual, by design.

Concretely: not "BTC will go up 50%," but "X of Y independent signals currently agree on a bullish read; regime moved NEUTRAL → EARLY BULL." Confidence over prediction, transition-detection over price-target-calling.

**What this was built from** (verified against source): whale flow, aggressive buy/sell flow, funding/OI, technical indicators (EMA trend/momentum), Fear&Greed, BTC dominance, per-signal ML confidence (`mlScoring.ts`), a 6-desk AI Council, multi-channel alerts with an existing circuit-breaker (`alertCooldown.ts`). All were real and already **per-asset** before this — the Regime Engine (below) is what combines them into a market-wide read.

**P0 — shipped (v9.25), see that changelog entry for the full audit**: `src/lib/regime/` (types/signals/engine/weights) + `useRegimeEngine.ts` + `RegimePanel.tsx`, live on the homepage. 10 real signals (BTC trend + momentum, market breadth, whale flow, aggressive flow, OI rate-of-change, funding, Fear&Greed level + 7d change, BTC dominance trend), weighted scoring into a 7-way classification (`BEAR → RECOVERY → NEUTRAL → EARLY BULL → BULL → LATE BULL → DISTRIBUTION`), real-elapsed-time persistence (5m/30m/2h deltas, confirmation after `PERSISTENCE_SNAPSHOTS` consecutive polls), an "agreeing signals" correlation count, a full "why am I seeing this" reasons list, and an in-panel weight-tuning UI. **v9.26 claimed, v9.28 actually delivered**: regime-transition alert delivery to Telegram/Discord (`regimeAlerts.ts`/`useRegimeAlerts.ts`) — v9.27 found the v9.26 claim didn't match source; v9.28 built the missing module and wired it into `RegimePanel` for real. **v9.27 added** the REGIME DESK agent to the AI Council, closing the first item off the "still genuinely missing" list below. **v9.29 closed two more**: a real cooldown/alert-loss bug in `useRegimeAlerts.ts` (found by external audit), and real market-wide breadth off Binance's top-150-by-volume USDT pairs instead of the user's own watchlist. **v9.33 closed signal-family grouping**: the 10 signals now aggregate into 5 independent families (Trend, Derivatives, Flow, Sentiment, Dominance), with a `families: FamilyReading[]` field on `RegimeReading`, a 🔗 FAMILIES badge in `RegimePanel`, and a "X of Y independent families agree" line in the reasons text — a more honest breadth read than the raw 10-signal count. **v9.34 closed backtesting**: new `src/lib/regime/backtest.ts` tests every confirmed regime call still in local history against real forward BTC price at 1h/4h/24h, on-demand from a new BACKTEST panel section — honestly bounded to whatever history has accumulated in the current browser session (`localStorage`, max 400 snapshots), not a multi-year replay. **v9.35 closed BTC/ETH relative strength**: an 11th signal, `eth_btc_strength`, reading Binance's ETHBTC pair directly; the "Dominance" family is renamed **"Rotation"** and now holds both `btc_dominance` and this new signal, since they're both answers to "which asset is capital moving into," just measured against a different denominator.

**What's still genuinely missing** (not built, not simulated as built):
- A **decision-outcome loop**: "Mark as Reviewed" / "I Bought" buttons that log the alert → the user's actual action → later P/L, so the ML can eventually learn *which alert types were actually useful to this specific user*.
- A **true server-side 24/7 regime engine**. `useRegimeEngine()` still only runs while a browser tab is open (`setInterval` in React state) — close the tab, let the phone suspend the browser, or let the PC sleep, and monitoring stops. Flagged by George's external audit (see v9.29): needs a server/edge cron collecting signals + computing regime + persisting state (Supabase/Postgres) + dispatching Telegram/Discord independent of any open tab, with the dashboard just reading the persisted state. This is also why v9.34's backtest is bounded to one browser session's local history rather than a real historical archive — there's no server-side store yet for it to backtest against.
- A **stablecoin/liquidity flow** signal — proposed addition to the regime engine's signal set, not yet built. (BTC/ETH relative strength, the other proposed addition, shipped in v9.35.)
- The **Early/Developing/Confirmed 3-tier persistence ladder** George's audit proposed in place of the current flat `confirmedRegime` flag — see "Open tuning questions" below.

**Closed since the audit that raised them**: signal-family grouping (v9.33), backtesting the regime engine's own calls (v9.34), and BTC/ETH relative strength (v9.35) — all three were on this list as of v9.29/v9.30, see the changelog entries above for what shipped.

**Real market breadth landed in v9.29** — `marketBreadthSignal()` in `signals.ts` now reads Binance's full 24hr ticker (top 150 USDT pairs by volume, non-leveraged), not the user's own scanned/watchlist coins; falls back to the old watchlist proxy (clearly labeled as a fallback) only if that fetch fails.

**Open tuning questions, deliberately not resolved unilaterally**: `PERSISTENCE_SNAPSHOTS = 3` at a 5-minute poll interval confirms a regime after 15 minutes — looser than this doc's original 5min → 30min → 2h persistence idea, and George's audit separately proposed splitting `confirmedRegime` into a 3-tier ladder (`EARLY` ~15-30min → `DEVELOPING` ~1-2h → `CONFIRMED` ~6-12h/daily) rather than one flat confirmation flag. Both left as deliberate decisions to make, not changed as a side effect of an audit.

**Consequence for existing execution features** (Nexus grid/arbitrage/volume-maker, Strategy Trader bridge): deprioritized, not removed. They keep working as-is; effort stops going toward polishing their execution-safety edges (arbitrage compensation, grid orphan-order handling, volume-maker safety) since they're no longer the point of the project. This also **changes the calculus on the 🔜 Planned AI-safety items below** (approval queue, tri-state AI mode, AI-specific risk caps, audit log) — those exist to make AI-to-execution wiring safe, and if execution is no longer the target output, they drop from "needed before Council-to-execution" to "only relevant if that connection gets built anyway later." Still listed below as real candidates, just no longer implied urgent by this pivot.

**Priority order, updated now that P0 is real**:
- P0: server-side 24/7 regime engine + persistent server-side regime history (both need a real architecture decision plus Supabase deploy access).
- P1 (current focus): decision-outcome loop, stablecoin/liquidity flow.
- P2: personal outcome learning feeding back into signal weights, adaptive weights from actual historical performance, homepage layout beyond the current panel placement, the execution features above (now optional, not core).



Both previously-open roadmap items are closed as of v9.7/v9.8 — see ✅ Done below. v9.9-v9.12 were separately-found gaps (mostly "logic exists, no UI consumer"; v9.12 was a fake-output variant of the same pattern), not roadmap items. v9.13/v9.14 were AI-discoverability additions (crawler-facing static shells, then an MCP tool server) rather than gap fixes. v9.15-v9.19 were bug-fix passes on those additions (Reddit/news upstream blocking, an MCP audit's findings, and a caching/upstream-load cleanup) rather than new gaps or roadmap items. v9.20 closed the push-notifications item that had been flagged as a real next candidate since v9.10. v9.21 was another small "no UI consumer" find (`getScanCoins()`), same family as v9.6/v9.9-v9.12. v9.22 closed the Solana pre-pump-transfer-detection gap's *transfer-detection* half — the CEX-address-list half is deliberately left as a manual step; see v9.22 in [What's New](#-whats-new) for why a hardcoded guess wasn't the right fix. v9.23 wasn't a new gap at all — a fresh pass over `src/lib/mcp/` (the area flagged as a next-candidate source) found v9.18's `list_council_decisions` exact-match fix had landed in an orphaned, never-imported duplicate file instead of the live one, so the substring-match bug was still shipping; corrected at the actual source, dead duplicate files removed. v9.24 swept `src/lib/nexus/` next: closed two more "no UI consumer" gaps in the protection engine (bulk lock-clearing, current-config visibility + reset), and traced `getManifest()`/`describeManifest()` — the one export in that sweep still uncalled — to a real architectural blocker (browser-localStorage-only state vs. a Supabase-Edge-Function MCP server with no path to it) rather than force-wiring something that wouldn't reach its intended audience anyway. One small real gap remains on record, not force-closed: `triggerBackgroundSync()` (`src/lib/pwa.ts`, no `sync` handler in `public/sw.js` and no offline-write queue to attach one to). A comparison against [PolyBot](https://github.com/cryptuon/polybot) (a more mature agent-trading framework) surfaced four genuinely new candidates around AI/Council-to-execution safety — see 🔜 Planned below; the most directly buildable of the four is the human-approval queue, since it sits on top of plumbing (`strategyTraderBridge.ts`) that already exists, though the pivot below has since made these lower-urgency (they exist to make AI-to-execution wiring safe, and execution is no longer the target output). v9.25 is the P0 Regime Engine described in the new 🧭 Strategic Direction section below — 10 real signals, weighted classification, real-elapsed-time persistence, a fixed weight-slider-persistence bug found during audit; see that changelog entry and the Strategic Direction section for what's real vs. still missing. v9.26 closed one item off that missing-list — regime-transition alerts now reach Telegram/Discord, gated on a real `confirmedRegime` change through the existing cooldown/notification pipeline. v9.27 closed a second item — the AI Council gained a REGIME DESK agent reading the live Regime Engine block — and along the way found that v9.26's `regimeAlerts.ts`/`useRegimeAlerts.ts` claim didn't match this checkout's actual source (module missing, hook orphaned/uncalled). v9.28 resolved that finding rather than leaving it open: built `regimeAlerts.ts` for real, deleted an orphaned duplicate `useRegimeAlerts.ts`, and wired the surviving one into `RegimePanel` — v9.26's original claim is now true. v9.29 acted on George's external audit of that v9.28 build: fixed a real cooldown/alert-loss bug the audit found, and replaced the watchlist-scoped breadth proxy with real market-wide breadth off Binance. v9.30 was a full repo-wide lint cleanup (129 eslint problems → 0 errors) rather than a feature or roadmap item — one genuine Rules-of-Hooks bug turned up in the process (`HLOpportunityPanel.tsx`), plus a separate real finding that needs a decision before it's fixed: the Insider Risk Scanner's "real data" path has been silently dead (always falling back to mock data) for an unknown amount of time, since `CoinData` never carries a contract address. v9.31 wasn't a new gap-finding pass finding something new so much as it was one — a fresh audit of `src/lib/council/` (flagged as a next-candidate source) found the AI Council was scoring coins against a private duplicate `getCeoSignalLabel()` with different thresholds than the one the scanner table actually shows the user; collapsed to one canonical function. v9.32 closed the v9.30 Insider Risk Scanner finding — not with the extra-CoinGecko-lookup fix the README itself had proposed, but a cheaper one found while looking: the existing bulk CoinGecko call already supports `include_platform=true`, adding contract addresses to every coin at zero extra request cost. v9.33 and v9.34 closed two of the four remaining Strategic Direction P1 items: signal-family grouping (5 independent families instead of 10 correlated raw signals) and backtesting the regime engine's own confirmed calls against real forward BTC price, both described in detail above. v9.35 closed a third — BTC/ETH relative strength, an 11th regime signal, folded into a renamed "Rotation" family alongside BTC dominance. Explicitly tried and deliberately not force-fixed along the way: a verified Solana CEX-address list for the v9.22 item — web search couldn't turn up a reliable, comprehensive source, and a half-verified list feeding a risk scanner is worse than the current honest empty one. Remaining candidates: a true server-side 24/7 regime engine (still the audit's #1 remaining architectural gap — needs Supabase deploy access this sandbox doesn't have), the decision-outcome loop, a stablecoin/liquidity flow signal, the Early/Developing/Confirmed 3-tier persistence ladder, a verified Solana CEX-address list (unresolved, see above), or a fresh gap-finding pass over `server/services/`, `server/routes/`, or `freqtrade-develop`.

🔜 Planned (found comparing against [PolyBot](https://github.com/cryptuon/polybot), a more mature agent-trading framework — none of these exist yet, listed here as real next candidates, not done):
- **Tri-state AI trading mode** (`disabled` / `shadow` / `live`) as an explicit setting, replacing the single global `isDryRun()` boolean in `bot.ts`. Matters especially because the AI Council (bull/bear/quant/risk/trader/PM) produces verdicts today with **zero connection to execution** — confirmed while researching this: `src/lib/council/` does no tool-calling and never references Nexus. A tri-state mode is the natural gate to have in place *before* that connection is ever made, not after.
- **Human approval queue for AI-initiated trades**, PolyBot's `mcp pending`/`approve <id>`/`reject <id>` pattern. Today `forwardSignalToStrategyTrader()` (`strategyTraderBridge.ts`, called from `WRRightPanel.tsx`) sends straight through once it clears cooldown/dry-run/the 50%-ML-confidence floor — those are automatic server-side gates, not a human-in-the-loop step. Needed before Council verdicts (see above) could ever be wired to real execution. Of the four items here, this is the one most directly buildable on existing plumbing: a "pending AI signal" state instead of an immediate `forwardSignalToStrategyTrader()` call, a small UI list with Approve/Reject, and the real send only firing on explicit click.
- **Audit log of every AI/MCP tool call**, not just completed trades. `botTradeStore.ts` only records finished trades; there's no record of "an MCP client invoked action X at time Y" regardless of outcome — PolyBot's `mcp audit`. Matters more once/if any AI-driven action path exists to audit.
- **AI-specific risk caps layered on top of the general Nexus protections**, mirroring PolyBot's `MCP_MAX_POSITION_USD`/`MCP_DAILY_LOSS_LIMIT_USD`. `protections.ts` gates a human clicking a button and an AI-initiated call identically today; a tighter, separate cap specifically for AI-driven actions (independent of whatever a human has configured for themselves) would need to exist before Council-to-execution wiring is worth doing at all.


- **Real web push notifications, end to end, including the auto-send-on-critical-alert follow-up** — see [What's New](#-whats-new) above.
- **Market Sentiment now calls real AI instead of a canned template** — see [What's New](#-whats-new) above.
- **Alert pin persistence** — see [What's New](#-whats-new) above.
- **PWA install prompt and cache clearing** — see [What's New](#-whats-new) above.
- **Scan history is now actually browsable** — see [What's New](#-whats-new) above.
- **Expand AI Council with additional agent personas and longer-horizon memory scoring** — new QUANT DESK agent persona (orderflow/derivatives positioning, `deep` depth only); `computeDeskTrackRecord()` gives the existing longest-horizon grading logic a structured, UI-visible form (0-100 score, hit rate, avg realized return) shown as a badge in `WRCouncilPanel`, where previously it was only text folded into the PM's prompt. Also fixed a latent bug found along the way: `council-persist`'s verdict allow-list had drifted from the real `CouncilVerdict` enum and was silently rejecting most decisions before they ever reached memory.
- **Smart-money wallet skill scoring** — `src/lib/walletSkillScoring.ts` parses each tracked wallet's recent swaps (SOL↔SPL-token balance deltas from `getTransaction`) and FIFO-matches buys/sells per mint into a win-rate + avg-profit-SOL skill score, polled by `useWalletSkillScoring`. See [What's New](#-whats-new) above for the exact scope and its honest limitations.
- **Live wallet tracking is now actually live** — `src/lib/solanaWallet.ts` fetches real SOL balance + recent transaction count/timing via public Solana RPC (`getBalance`/`getSignaturesForAddress`), polled every 45s by `useWalletActivity`. `WalletEntry.lastActivity` existed as a field since before this work started and was never populated; the tracker tab showed a static labeled address list with no real activity behind it.
- `src/lib/nexus/pairPerformance.ts`'s `rankByPerformance()` now orders the watchlist bar (`WRTracker.tsx`) by historical performance.
- `src/lib/mlScoring.ts`'s `predictConfidence()` is now a live per-coin "🧠N%" badge in the scanner table (`WRScanner.tsx`), alongside a win-rate badge from `getSymbolPerformance()`.
- `mcp-nexus-bot/` — an MCP server wrapping `/api/nexus-bot/*`, so an MCP-compatible AI client (Claude Desktop, Claude Code, ...) can drive Nexus directly with the same server-side gates the browser bridge goes through. See its own [README](mcp-nexus-bot/README.md).
- A Freqtrade REST API bridge as a second, complementary bot — see [Strategy Trader](#strategy-trader--a-second-complementary-bot).
- freqtrade's own pair-lock list is now visible in Settings → 🎯 STRATEGY TRADER, with a per-lock CLEAR action (`GET`/`DELETE /strategy-trader/locks`).
- Grid *maintenance* (re-placing an order once a level fills) — implemented in `server/services/nexusBotWorker.ts`, polling every `NEXUS_GRID_POLL_MS`.
- Volume Maker's real trading loop — `VolumeMakerOpts` now carries `exchange`/`symbol`; the worker runs a small "ping-pong" tick against them (`NEXUS_VOLUME_MAKER_TICK_USD`).
- Real grid PNL — `server/services/gridPnl.ts` FIFO-matches sells against the oldest unmatched buys; `GET /grids` reports the running total instead of a hardcoded 0.
- Pairlist-style pre-filters already exist: `src/lib/pairFilters.ts` (whale-feed) and `src/lib/nexus/pairQuality.ts` (bot gate) — ports of freqtrade's RangeStabilityFilter/VolatilityFilter/SpreadFilter/AgeFilter/PerformanceFilter/RemotePairList, the last two reusing `pairPerformance.ts` and `remotePairList.ts` directly rather than re-deriving the logic.
- Sortino/Calmar/SQN now computed in `src/lib/backtestMetrics.ts` alongside profit-factor/Sharpe/drawdown.
- `src/lib/nexus/pairPerformance.ts` — PerformanceFilter-style ranking of pairs by historical signal performance.
- `src/lib/nexus/openTradesLimit.ts` — FullTradesFilter-style gate that shrinks available trade slots when the bot's concurrent-position cap is reached.
- `src/lib/nexus/protectionOptimizer.ts` — hyperopt-style local grid-search that suggests protection thresholds from the bot's own closed-trade history.
- `src/lib/nexus/remotePairList.ts` — RemotePairList-style fetch of a curated/shared symbol list from an external JSON URL, now actually gating the whale-radar scan (`remoteWhitelistFilter`) rather than just displaying a fetched count.
- Dry-run mode for the Nexus Bot guarded-execution wrappers (`bot.ts`) — simulates protection gating without placing real orders.
- Multi-channel alerts (Telegram, Discord) beyond the in-app bell — `src/lib/notifyChannels.ts`, configurable per-channel severity floor, wired into `WRSettingsPanel`.
- `src/lib/alertCooldown.ts` — StoplossGuard/MaxDrawdownProtection-style per-symbol and global circuit breaker on the whale-radar alert feed itself (separate from the Nexus Bot's own `protections.ts`).
- `src/lib/mlScoring.ts` — FreqAI-lite adaptive confidence scoring trained on this app's own recorded outcomes.
- `src/lib/sizingHint.ts` — Edge-Positioning-lite expectancy-based sizing hints, shown on live alerts.
- OKX and Kraken added as live whale-feed exchange sources (`src/lib/exchanges/`), both wired into the exchange filter tabs.

---

## ⚠️ Disclaimer

Educational and informational purposes only. Not financial or trading advice. Do your own research before making any trading decisions.
